---
title: टियरडाउन
description: >-
  सभी Azure संसाधनों को हटाने के लिए terraform destroy, az CLI से क्लीनअप
  सत्यापित करें, स्थानीय स्टेट फ़ाइलें साफ़ करें, और संबद्ध F5 XC ऑरिजिन पूल और
  HTTP लोड बैलेंसर हटाएं
sidebar:
  order: 7
i18n:
  sourceHash: f4bb422b2c48
  translator: machine
---

जब लैब सत्र पूर्ण हो जाए, तो बिलिंग रोकने के लिए सभी Azure संसाधनों को नष्ट करें।

## Terraform Destroy

Terraform कॉन्फ़िगरेशन वाली डायरेक्टरी से:

```bash
terraform plan -destroy

terraform destroy
```

Terraform पुष्टि के लिए संकेत देगा। आगे बढ़ने के लिए `yes` टाइप करें।

## क्लीनअप सत्यापित करें

पुष्टि करें कि सभी संसाधन हटा दिए गए हैं:

```bash
az group show --name "$(terraform output -raw resource_group_name)" 2>&1 \
  | grep -q "ResourceGroupNotFound" \
  && echo "Resource group deleted" \
  || echo "Resource group still exists"
```

## स्थानीय स्टेट साफ़ करें

Terraform स्टेट और कैश की गई फ़ाइलें हटाएं:

```bash
rm -rf .terraform .terraform.lock.hcl terraform.tfstate terraform.tfstate.backup
```

## F5 XC क्लीनअप

यदि आपने इस ऑरिजिन सर्वर की ओर संकेत करने वाले F5 XC संसाधन बनाए हैं, तो उन्हें हटाएं:

1. HTTP लोड बैलेंसर हटाएं
2. ऑरिजिन पूल हटाएं
3. परीक्षण के लिए बनाई गई कोई भी WAF नीतियां हटाएं
