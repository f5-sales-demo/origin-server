---
title: الإزالة
description: >-
  استخدام terraform destroy لإزالة جميع موارد Azure، والتحقق من التنظيف باستخدام
  az CLI، وتنظيف ملفات الحالة المحلية، وإزالة خادم المصدر المرتبط وموازن تحميل
  HTTP في F5 XC
sidebar:
  order: 7
i18n:
  sourceHash: f4bb422b2c48
  translator: machine
---

عند اكتمال جلسة المختبر، قم بإزالة جميع موارد Azure لإيقاف الفوترة.

## إزالة Terraform

من الدليل الذي يحتوي على تكوين Terraform:

```bash
terraform plan -destroy

terraform destroy
```

سيطلب Terraform التأكيد. اكتب `yes` للمتابعة.

## التحقق من التنظيف

تأكيد إزالة جميع الموارد:

```bash
az group show --name "$(terraform output -raw resource_group_name)" 2>&1 \
  | grep -q "ResourceGroupNotFound" \
  && echo "Resource group deleted" \
  || echo "Resource group still exists"
```

## تنظيف الحالة المحلية

إزالة حالة Terraform والملفات المؤقتة:

```bash
rm -rf .terraform .terraform.lock.hcl terraform.tfstate terraform.tfstate.backup
```

## تنظيف F5 XC

إذا قمت بإنشاء موارد F5 XC تشير إلى خادم المصدر هذا، قم بإزالتها:

1. احذف موازن تحميل HTTP
2. احذف مجموعة المصدر
3. احذف أي سياسات جدار حماية تطبيقات الويب (WAF) تم إنشاؤها للاختبار
