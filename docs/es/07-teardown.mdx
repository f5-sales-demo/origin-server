---
title: Desmontaje
description: >-
  terraform destroy para eliminar todos los recursos de Azure, verificar la
  limpieza con az CLI, limpiar los archivos de estado locales y eliminar el
  grupo de origen y el balanceador de carga HTTP de F5 XC asociados
sidebar:
  order: 7
i18n:
  sourceHash: f4bb422b2c48
  translator: machine
---

Cuando la sesión de laboratorio haya concluido, destruya todos los recursos de Azure para detener la facturación.

## Terraform Destroy

Desde el directorio que contiene la configuración de Terraform:

```bash
terraform plan -destroy

terraform destroy
```

Terraform solicitará confirmación. Escriba `yes` para continuar.

## Verificar la limpieza

Confirme que todos los recursos han sido eliminados:

```bash
az group show --name "$(terraform output -raw resource_group_name)" 2>&1 \
  | grep -q "ResourceGroupNotFound" \
  && echo "Resource group deleted" \
  || echo "Resource group still exists"
```

## Limpiar el estado local

Elimine el estado de Terraform y los archivos en caché:

```bash
rm -rf .terraform .terraform.lock.hcl terraform.tfstate terraform.tfstate.backup
```

## Limpieza de F5 XC

Si creó recursos de F5 XC que apuntan a este Servidor de origen, elimínelos:

1. Elimine el balanceador de carga HTTP
2. Elimine el grupo de origen
3. Elimine cualquier política WAF creada para pruebas
