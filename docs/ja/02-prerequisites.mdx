---
title: 前提条件
description: >-
  Azureサブスクリプション、Terraform >= 1.5、Azure
  CLI、SSHキーペア、および41コンテナデプロイ用のStandard_D16s_v3 VM（16 vCPU、64 GiB RAM）
sidebar:
  order: 2
i18n:
  sourceHash: 293ed3db6122
  translator: machine
---

## 必須要件

### Azureサブスクリプション

以下のリソースを作成する権限を持つ有効なAzureサブスクリプションが必要です：

- リソースグループ
- 仮想ネットワークおよびサブネット
- ネットワークセキュリティグループ
- パブリックIPアドレス
- 仮想マシン（Standard_D16s_v3 -- 41コンテナワークロード用に16 vCPU、64 GiB RAM）

### Azure CLI

Azure CLIをインストールして認証を行います：

```bash
curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash

az login

az account show --query '{name:name, id:id}' -o table
```

### Terraform

Terraform >= 1.5 が必要です：

```bash
terraform version
```

インストールされていない場合は、HashiCorp APTリポジトリを使用してください：

```bash
wget -O- https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
sudo apt update && sudo apt install terraform
```

### Azure AD権限

Azure ADアカウントには `User.Read` 権限が必要です。デプロイヤー識別子（リソース名に使用）は、Azure ADプロファイルから自動的に導出されます。サービスプリンシパルまたはマネージドIDによる認証の場合は、`deployer` Terraform変数を明示的に設定してください。

### SSHキーペア

VMアクセス用のSSHキーペアが必要です：

```bash
ssh-keygen -t ed25519 -f ~/.ssh/origin-server-key -N ""
```

## 作成されるAzureリソース

| リソース | SKU |
|---|---|
| Ubuntu 24.04 VM | Standard_D16s_v3（16 vCPU、64 GiB） |
| パブリックIP | Standard、Static |
| OSディスク | 60 GiB Premium SSD |
| VNet + NSG | デフォルト |

現在のコストについては、[Azure料金計算ツール](https://azure.microsoft.com/en-us/pricing/calculator/)を参照してください。ラボを使用しない場合は `terraform destroy` を実行して課金を停止してください。手順については[ティアダウン](../07-teardown/)を参照してください。

:::note
Standard_D16s_v3（64 GiB RAM）は41コンテナデプロイに必要です。コンテナのメモリ割り当て：Juice Shopインスタンス4つ（各1 GiB、計4 GiB）、VAmPI 4つ（各512 MiB、計2 GiB）、DVWA-FPM + MariaDB 4つ（1.8 GiB）、httpbin 4つ（256 MiB、計1 GiB）、CSD Demo 4つ（256 MiB、計1 GiB）、whoami 4つ（64 MiB、計256 MiB）、DVGA 4つ（各256 MiB、計1 GiB）、RESTaurant + PostgreSQL 4つ（1.5 GiB）、crAPI 7マイクロサービス（2 GiB）、さらにnginx、OS、およびバッファキャッシュ。コンテナの合計割り当ては約14.5 GiB（64 GiBの22.7%）です。
:::

:::note
NSGはポート80（HTTP）およびポート8888（crAPI）へのインバウンドトラフィックを許可します。ポート8888が必要なのは、crAPIがシングルページアプリケーションであり、APIパスをハードコードしているため、パスプレフィックスの背後からサービスを提供できないためです。
:::
