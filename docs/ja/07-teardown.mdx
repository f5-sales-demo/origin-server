---
title: ティアダウン
description: >-
  terraform destroy を使用してすべての Azure リソースを削除し、az CLI
  でクリーンアップを確認し、ローカルステートファイルを削除し、関連する F5 XC オリジンプールおよび HTTP ロードバランサーを削除する
sidebar:
  order: 7
i18n:
  sourceHash: f4bb422b2c48
  translator: machine
---

ラボセッションが完了したら、課金を停止するためにすべての Azure リソースを破棄します。

## Terraform Destroy

Terraform 設定が含まれるディレクトリから実行します。

```bash
terraform plan -destroy

terraform destroy
```

Terraform は確認を求めるプロンプトを表示します。続行するには `yes` と入力してください。

## クリーンアップの確認

すべてのリソースが削除されたことを確認します。

```bash
az group show --name "$(terraform output -raw resource_group_name)" 2>&1 \
  | grep -q "ResourceGroupNotFound" \
  && echo "Resource group deleted" \
  || echo "Resource group still exists"
```

## ローカルステートのクリーンアップ

Terraform のステートファイルおよびキャッシュファイルを削除します。

```bash
rm -rf .terraform .terraform.lock.hcl terraform.tfstate terraform.tfstate.backup
```

## F5 XC のクリーンアップ

このオリジンサーバーを参照する F5 XC リソースを作成した場合は、それらを削除してください。

1. HTTP ロードバランサーを削除する
2. オリジンプールを削除する
3. テスト用に作成した WAF ポリシーを削除する
