---
title: 清除環境
description: >-
  使用 terraform destroy 移除所有 Azure 資源、以 az CLI 驗證清除結果、清理本地狀態檔案，並移除相關的 F5 XC 來源池與
  HTTP 負載平衡器
sidebar:
  order: 7
i18n:
  sourceHash: f4bb422b2c48
  translator: machine
---

實驗課程結束後，請銷毀所有 Azure 資源以停止計費。

## Terraform 銷毀

在包含 Terraform 設定的目錄中執行：

```bash
terraform plan -destroy

terraform destroy
```

Terraform 將提示確認。輸入 `yes` 以繼續。

## 驗證清除結果

確認所有資源已被移除：

```bash
az group show --name "$(terraform output -raw resource_group_name)" 2>&1 \
  | grep -q "ResourceGroupNotFound" \
  && echo "Resource group deleted" \
  || echo "Resource group still exists"
```

## 清理本地狀態

移除 Terraform 狀態及快取檔案：

```bash
rm -rf .terraform .terraform.lock.hcl terraform.tfstate terraform.tfstate.backup
```

## F5 XC 清除

若您已建立指向此來源伺服器的 F5 XC 資源，請將其移除：

1. 刪除 HTTP 負載平衡器
2. 刪除來源池
3. 刪除任何為測試而建立的 WAF 政策
