---
title: Démantèlement
description: >-
  terraform destroy pour supprimer toutes les ressources Azure, vérifier le
  nettoyage avec az CLI, nettoyer les fichiers d'état locaux, et supprimer le
  pool d'origine et l'équilibreur de charge HTTP F5 XC associés
sidebar:
  order: 7
i18n:
  sourceHash: f4bb422b2c48
  translator: machine
---

Lorsque la session de laboratoire est terminée, détruisez toutes les ressources Azure pour arrêter la facturation.

## Destruction Terraform

Depuis le répertoire contenant la configuration Terraform :

```bash
terraform plan -destroy

terraform destroy
```

Terraform vous demandera une confirmation. Tapez `yes` pour continuer.

## Vérifier le nettoyage

Confirmez que toutes les ressources ont été supprimées :

```bash
az group show --name "$(terraform output -raw resource_group_name)" 2>&1 \
  | grep -q "ResourceGroupNotFound" \
  && echo "Resource group deleted" \
  || echo "Resource group still exists"
```

## Nettoyer l'état local

Supprimez l'état Terraform et les fichiers mis en cache :

```bash
rm -rf .terraform .terraform.lock.hcl terraform.tfstate terraform.tfstate.backup
```

## Nettoyage F5 XC

Si vous avez créé des ressources F5 XC pointant vers ce serveur d'origine, supprimez-les :

1. Supprimez l'équilibreur de charge HTTP
2. Supprimez le pool d'origine
3. Supprimez toutes les politiques WAF créées à des fins de test
