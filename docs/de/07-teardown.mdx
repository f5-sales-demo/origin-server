---
title: Abbau
description: >-
  terraform destroy zum Entfernen aller Azure-Ressourcen, Überprüfung der
  Bereinigung mit az CLI, Bereinigung lokaler Statusdateien und Entfernen des
  zugehörigen F5 XC Ursprungsserver-Pools und HTTP-Lastverteilers
sidebar:
  order: 7
i18n:
  sourceHash: f4bb422b2c48
  translator: machine
---

Wenn die Labsitzung abgeschlossen ist, löschen Sie alle Azure-Ressourcen, um die Abrechnung zu stoppen.

## Terraform Destroy

Aus dem Verzeichnis, das die Terraform-Konfiguration enthält:

```bash
terraform plan -destroy

terraform destroy
```

Terraform fordert zur Bestätigung auf. Geben Sie `yes` ein, um fortzufahren.

## Bereinigung überprüfen

Bestätigen Sie, dass alle Ressourcen entfernt wurden:

```bash
az group show --name "$(terraform output -raw resource_group_name)" 2>&1 \
  | grep -q "ResourceGroupNotFound" \
  && echo "Resource group deleted" \
  || echo "Resource group still exists"
```

## Lokalen Status bereinigen

Entfernen Sie den Terraform-Status und zwischengespeicherte Dateien:

```bash
rm -rf .terraform .terraform.lock.hcl terraform.tfstate terraform.tfstate.backup
```

## F5 XC Bereinigung

Wenn Sie F5 XC-Ressourcen erstellt haben, die auf diesen Ursprungsserver verweisen, entfernen Sie diese:

1. Löschen Sie den HTTP-Lastverteiler
2. Löschen Sie den Ursprungsserver-Pool
3. Löschen Sie alle WAF-Richtlinien, die zu Testzwecken erstellt wurden
