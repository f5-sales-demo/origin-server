---
title: การรื้อถอน
description: >-
  terraform destroy เพื่อลบทรัพยากร Azure ทั้งหมด, ตรวจสอบการล้างข้อมูลด้วย az
  CLI, ล้างไฟล์สถานะภายในเครื่อง, และลบ origin pool และ HTTP load balancer ของ
  F5 XC ที่เกี่ยวข้อง
sidebar:
  order: 7
i18n:
  sourceHash: f4bb422b2c48
  translator: machine
---

เมื่อเซสชันแล็บเสร็จสิ้นแล้ว ให้ทำลายทรัพยากร Azure ทั้งหมดเพื่อหยุดการเรียกเก็บค่าใช้จ่าย

## Terraform Destroy

จากไดเรกทอรีที่มีการกำหนดค่า Terraform:

```bash
terraform plan -destroy

terraform destroy
```

Terraform จะขอการยืนยัน พิมพ์ `yes` เพื่อดำเนินการต่อ

## ตรวจสอบการล้างข้อมูล

ยืนยันว่าทรัพยากรทั้งหมดถูกลบออกแล้ว:

```bash
az group show --name "$(terraform output -raw resource_group_name)" 2>&1 \
  | grep -q "ResourceGroupNotFound" \
  && echo "Resource group deleted" \
  || echo "Resource group still exists"
```

## ล้างสถานะภายในเครื่อง

ลบไฟล์สถานะ Terraform และไฟล์แคช:

```bash
rm -rf .terraform .terraform.lock.hcl terraform.tfstate terraform.tfstate.backup
```

## การล้างข้อมูล F5 XC

หากคุณสร้างทรัพยากร F5 XC ที่ชี้ไปยัง origin server นี้ ให้ลบออก:

1. ลบ HTTP load balancer
2. ลบ origin pool
3. ลบนโยบาย WAF ที่สร้างขึ้นสำหรับการทดสอบ
